Behavior = BHV_Timer
{
  name      = timer
  condition = MODE == ACTIVE
  condition = TIMER_ACTIVE = true

  var_status_idle    = TIMER_IDLE_SECONDS
  var_status_running = BAD VAR
  status_suffix      = base
}
