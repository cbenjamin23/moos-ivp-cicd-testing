//-------------------------------------------------
// FILE: max-depth-bad-slack-var-fail-vehicle.xbhv
// NAME: Charles Benjamin
//-------------------------------------------------

Behavior = BHV_Waypoint
{
  name      = transit
  pwt       = 80
  condition = MODE == TRANSITING
  endflag   = ARRIVED = true

  perpetual = false
  speed     = $(STOCK_SPD)

  capture_line   = true
  capture_radius = 4.0
  slip_radius    = 12.0
  points         = $(DEST_POS)
  repeat         = 0
}

Behavior = BHV_MaxDepth
{
  name      = max_depth
  pwt       = 300
  condition = MODE == TRANSITING

  max_depth       = 12
  tolerance       = 4
  depth_slack_var = BAD VAR
}
