# Harness Case Description Review Plan

## Goal

Review the case descriptions for all 67 configured harness targets and 1,541
cases so each description states:

1. the unique input, configuration, event, or code path under test; and
2. the behavior or contract the case is intended to exercise; and
3. the concrete observation that actually determines whether the case passes.

The harness README is the source of truth. The website generator extracts case
descriptions from the README case bullets, so generated
`docs/harnesses/*.html` files should not be edited by hand.

## Writing Standard

Prefer one or two short sentences in this form:

```text
<Unique setup or stimulus>, exercising <specific behavior or contract>;
passes when <specific observable and value>.
```

Good harness evidence includes exact result fields, MOOS variables, posted
values, counters, state transitions, timing bounds, tolerances, process exits,
or expected absences. Use the evidence that actually controls the verdict.

Example:

```text
Places one obstacle in the center of the transit lane to exercise normal
avoidance. Passes when arrived=true, encounters=1, and collisions=0.
```

For expected-negative scenarios, distinguish the harness verdict from the
mission outcome:

```text
Disables obstacle-alert delivery to exercise the no-alert path. The harness
passes when the vehicle arrives after one encounter and one collision.
```

Avoid vague endings such as "works," "behaves correctly," "arrives cleanly,"
"produces a usable result," or "passes as expected" when the evaluator exposes
a more precise condition. Do not invent a criterion from a case name; confirm
it in the case patches, mission evaluator configuration, launcher checks, or a
representative result row.

Use **tests** or **verifies** only when the grading condition distinguishes the
intended behavior from a plausible regression. Use **exercises** when the case
enters a configuration or code path but its evaluator checks only a broader
outcome. Record that mismatch in the coverage-gap table rather than making the
description claim more than the case proves.

## Two-Pass Approach

### Pass 1: description and evidence audit

Review every case against its launcher mapping, patches, evaluator, and a
representative result. Rewrite the README description to state the exact setup,
intended behavior, and actual grading evidence. Add any insufficient or
non-discriminating evaluation to the coverage-gap table below.

### Pass 2: case strengthening

Use the coverage-gap table as the worklist for later test changes. Prefer
discriminating fixtures, exact payload or state comparisons, and mutation
probes that demonstrate the case fails when the intended behavior is removed.
After strengthening a case, rerun it and update its description from
**exercises** to **tests** or **verifies** when warranted.

## Completion Criteria

A harness is complete when:

- every case exposed by `zlaunch.sh` has one matching README description;
- each description identifies what is unique about the case;
- each description names the actual pass/fail evidence;
- expected-negative cases clearly explain why `grade=pass` is correct;
- repetitive setup details are omitted unless they distinguish the case;
- the generated harness page contains the revised wording; and
- repository invariants pass.

## Review Workflow

1. Open the harness `zlaunch.sh`, README, relevant case patches, evaluation
   configuration, and representative `results.txt` rows.
2. Confirm that the README case inventory matches the launcher case inventory.
3. Rewrite the README case bullets using the writing standard above.
4. Run focused cases when the evaluator evidence is unclear or the description
   would otherwise rely on an assumption.
5. Regenerate the website with `python3 docs/tools/build_pages.py`.
6. Verify the generated harness page, then run
   `python3 scripts/check_repo_invariants.py`.
7. Check the README and Page boxes below.

The README box means the full case inventory was reviewed against actual test
evidence. The Page box means the generated page was regenerated and checked.

## Pass 2 Attack Plan

The coverage-gap table is a triage backlog, not a requirement to preserve and
expand every existing case. Before changing a fixture or evaluator, assign the
gap one of these dispositions:

- **Strengthen harness** when live MOOS mail, process interaction, helm state,
  vehicle motion, or timing is essential to the contract.
- **Move or add CTest coverage** when the intended contract is a deterministic
  parser, calculation, state transition, or payload-construction rule that can
  be demonstrated directly without a mission.
- **Merge or remove** when another case already proves the same contract, the
  fixture does not reach a distinct code path, or the behavior is not an
  intended public contract.
- **Keep as an exercise** when the scenario remains useful integration
  coverage but stronger grading would be disproportionately brittle. Retain
  the honest wording and document why broader evidence is acceptable.
- **Defer** when the contract first needs an upstream design decision,
  instrumentation, or stable performance envelope.

Use the Status column to record `Triaged: strengthen`, `Triaged: CTest`,
`Triaged: merge/remove`, `Triaged: keep`, or `Deferred`, followed by `Resolved`
only after the chosen change and its validation are complete.

### Decision Rules

Prioritize a finding when a plausible implementation regression can currently
pass, especially for a core app contract. Prefer exact payloads, counts,
ordered state evidence, numeric tolerances, and explicit absence flags over
additional end-state assertions.

Before retaining a case, answer all four questions:

1. What unique contract or failure mode would disappear if this case were
   removed?
2. Is a mission harness the smallest reliable layer that can prove it?
3. What deliberate mutation or broken fixture should make the improved case
   fail?
4. Can the evidence remain deterministic across supported systems and normal
   time-warps?

If question 1 has no specific answer, merge or remove the case. If question 2
is no, prefer CTest. If questions 3 or 4 have no practical answer, keep the
case only as an explicitly broad exercise or defer it.

### Execution Waves

| Wave | Scope | Why it comes here | Exit condition |
| --- | --- | --- | --- |
| 0 | Obstacle Manager pilot: `obmgr_h01` and `obmgr_h02` (7 gap rows) | Small, familiar family containing payload, absence, geometric, and motion-evidence problems. It will validate the disposition process before broad edits. | Every gap has a disposition; retained tests fail under a relevant mutation; focused cases and both family matrices pass. |
| 1 | Core state and control: Contact Manager, PID, and uSimMarine (22 gap rows) | These gaps can hide false passes in contact processing, override/stale transitions, actuator publication, and simulator state. | Pre/post state and contact-processing evidence distinguish the intended path from plausible no-op regressions. |
| 2 | Reports and transport: pNodeReporter, pShare, and uField communications (17 gap rows) | Payload identity, direction, route ordering, and report timing are central and usually measurable without motion tolerances. | Payloads and directional counts are checked independently; runtime ordering is graded rather than inferred from final state. |
| 3 | CLI and mission utilities | This is the best place to remove redundant cases and move deterministic parsing contracts to CTest. | Each retained case covers a distinct option, type, timing, or process contract; duplicates are consolidated. |
| 4 | Behavior and depth matrices | These are the largest groups and need behavior-specific motion or publication evidence rather than generic arrival/stability grades. | Each retained parameter case has a discriminating observable or is explicitly retained as broad integration coverage. |
| 5 | COLREGS, performance, and special-purpose harnesses | Threshold, geometry, and endurance evidence needs the most careful portability and stability review. | Boundary cases prove the intended side of each threshold, and performance checks use stable, justified envelopes. |

### Wave 0: Obstacle Manager Pilot

Work through the seven existing Obstacle Manager findings in this order:

1. Strengthen `disable_obstacle_pass`, `enable_obstacle_pass`, and
   `expunge_obstacle_pass` by comparing the complete `BHV_ABLE_FILTER` payload;
   record expunge removal as an upstream dependency when the checked-out app
   does not perform it for the documented bare-ID input.
2. Replace value-specific absence checks with a flag that detects any
   `OBM_DIST_TO_OBJ` publication in the seven expected-absence cases.
3. Extend `given_max_duration_missing_absent_pass` to reject any unexpected
   `OBSTACLE_ALERT` publication.
4. Redesign `max_pts_per_cluster_trim_pass` with point geometry whose trimmed
   and untrimmed hulls produce different observable results. If the retained
   point set cannot be observed reliably through the app, move the trimming
   contract to CTest and keep at most one broad point-cluster harness case.
5. Remove H02 `lasso_cluster_pass` because it has no lasso-specific evidence;
   retain the H01 lasso case that distinguishes the generated pseudo-hull from
   the non-lasso collinear-point fallback.

For each change, first demonstrate that the current evaluator accepts the
targeted broken condition or explain statically why it can. Then implement the
smallest discriminating check, run the focused case in minimal and full logging
when supported, run the complete family matrix, update the README description
and gap status, regenerate the website and dependency map, and run repository
invariants.

### Wave 0 Progress (2026-07-19)

- Exact `BHV_ABLE_FILTER` comparisons now grade the disable, enable, and
  expunge translation cases. A deliberately wrong action makes the focused
  case fail.
- All seven distance-absence cases now fail on any `OBM_DIST_TO_OBJ` mail. A
  mutation posting `WRONG_VALUE,999.0` fails, where the prior value-specific
  evaluator would have passed.
- `given_max_duration_missing_absent_pass` now fails on any alert mail. An
  injected unexpected alert makes the focused case fail.
- `max_pts_per_cluster_trim_pass` now places the first two points 10 meters
  away and the retained final three 22 meters away. The case requires the
  22-meter result; raising the cap from 3 to 20 retains the near points,
  reports 10 meters, and makes the case fail without depending on polygon
  vertex serialization. `OBM_MIN_DIST_EVER` remains diagnostic rather than a
  verdict because it correctly remembers the older near points.
- The complete 30-case H01 matrix passes with the strengthened evaluators in
  minimal logging, and representative cases pass with full logging.
- Source inspection found that a bare `OBM_EXPUNGE=ob_near` publishes the
  expunge filter but does not populate the ID passed to the erase path. The
  local case now states and proves only filter translation; the removal defect
  is parked under Suggested Upstream Changes.
- H02 `lasso_cluster_pass` was removed because it duplicated H01 lasso
  generation plus H02 generated-hull behavior spawning without adding
  lasso-specific evidence.

### Wave 1 Progress (2026-07-19)

- Contact Manager H01 expected-absence cases now prove that the excluded
  contact was still tracked by requiring closest-contact identity plus a
  case-specific range or list condition.
- The all-ranges and closest-relative-bearing cases use fixed ownship and
  stationary-contact geometry, then require the complete sorted range report
  and numeric relative bearing. Geometry and heading mutations make the
  focused cases fail.
- The contact enable and disable translation cases require the complete
  `BHV_ABLE_FILTER` payload; a wrong-action mutation fails.
- `detect_short_duration_absent_pass` was removed because a near-contact
  mutation alerted immediately, proving that its original pass came only from
  being out of range rather than from the short report duration.
- The complete 32-case H01 matrix passes, along with five representative cases
  in full logging.
- H02 no-alert cases now require closest-contact evidence, stale/reappear clears
  the old detection latch before the second report, and the two-contact case
  grades both the complete known-contact list and `alpha` as the alert source.
- H02 runtime re-enable uses short-lived `early` and post-enable `late`
  contacts with contact-specific alert flags. Ignoring the disable request
  latches a forbidden early alert and fails its focused mutation.
- The non-discriminating H02 helm-hold case was removed. The stem helm can
  enter DRIVE before a patch-driven contact, so this startup gate belongs in a
  direct Contact Manager test with controlled mail ordering rather than a
  timing-sensitive moving harness.
- Source inspection found the same bare-ID bookkeeping defect for Contact
  Manager enable/disable mail. The local translation cases remain independent,
  and the state-check follow-up is parked under Suggested Upstream Changes.
- PID H01 stale and override cases now grade the ordered control, suppression,
  and recovery states they claim to test. Refreshing stale mail, ignoring
  override, or omitting fresh post-release mail makes the corresponding case
  fail.
- H01 heading diagnostics are required rather than report-only, and alternate
  output suffixes now fail on any unsuffixed rudder or thrust publication.
- PID H02 now observes the four-second rudder disturbance from
  `sim_instability=18`, proves the vehicle remains stopped before override
  release, and requires both capped elevator output and partial depth response
  in the low-authority case. Removing each distinguishing configuration makes
  its focused case fail.
- uSimMarine now grades configured start depth, proves disabled-state nav mail
  was consumed by reenabling and moving from the seeded pose, and observes
  dual-state separation before `USM_RESET_NAV` resynchronizes the records.
  Removing each seed or transition makes its focused case fail.
- The uSim harness now forwards `--mmod` to the copied mission, so all result
  rows identify their actual case instead of reporting the baseline label.

## Suggested Upstream Changes

This is a parking lot for MOOS-IvP changes discovered while strengthening the
harnesses. These findings do not authorize edits to the upstream checkout, and
the local test matrix must remain meaningful and passing without those edits.

| ID | Upstream area | Observed behavior | Suggested change | Harness follow-up after upstream change | Status |
| --- | --- | --- | --- | --- | --- |
| `OBM-UP-001` | `pObstacleMgr` bare-ID enable/disable/expunge parsing | `OBM_EXPUNGE=ob_near` publishes `BHV_ABLE_FILTER=obstacle_id=ob_near,action=expunge`, but the bare-ID branch does not populate the local obstacle ID used by `addExpungedObstacle()`, so the managed obstacle is not erased. | Populate the parsed obstacle ID for the documented bare-ID form before applying enable, disable, or expunge bookkeeping. | Extend `expunge_obstacle_pass` with post-expunge evidence that no further output for `ob_near` is published; consider equivalent state checks for bare-ID enable and disable bookkeeping. | Deferred |
| `CMGR-UP-001` | `pContactMgrV20` bare-ID enable/disable parsing | `CONTACT_DISABLE=intruder` and `CONTACT_ENABLE=intruder` publish the correct `BHV_ABLE_FILTER`, but the bare-ID branch does not populate the local contact ID used by enable/disable bookkeeping and configured flag macros. | Populate the parsed contact ID for the documented bare-ID form before applying enable or disable bookkeeping. | Keep the current translation cases passing independently; after an upstream fix, add state or flag evidence proving the named contact entered the expected bookkeeping list. | Deferred |

## Coverage Gaps

These are documentation-audit findings, not failures introduced by this work.
They are intentionally deferred to the case-strengthening pass.

| Harness | Case | Intended property | Current grading evidence | Gap | Recommended strengthening | Status |
| --- | --- | --- | --- | --- | --- | --- |
| obmgr_h01 | `max_pts_per_cluster_trim_pass` | `max_pts_per_cluster=3` discards two older points 10 meters away and builds the hull from the final three points 22 meters away. | The evaluator requires the current `OBM_DIST_TO_OBJ=ROCK_A,22.0`; the lifetime minimum remains diagnostic because it remembers the discarded near points. | Resolved: an untrimmed cluster retains the near points and reports 10 meters, while equivalent polygon serialization changes do not affect the verdict. | Retain the separated geometry and current-distance check. | Resolved |
| obmgr_h01 | `disable_obstacle_pass` | `OBM_DISABLE=ob_near` becomes `BHV_ABLE_FILTER=obstacle_id=ob_near,action=disable`. | The evaluator now requires the complete expected payload through `OBM_EXPECTED_PAYLOAD`. | Resolved: unrelated or malformed filter mail no longer passes. | Retain the exact comparison. | Resolved |
| obmgr_h01 | `enable_obstacle_pass` | `OBM_ENABLE=ob_near` becomes `BHV_ABLE_FILTER=obstacle_id=ob_near,action=enable`. | The evaluator now requires the complete expected payload through `OBM_EXPECTED_PAYLOAD`. | Resolved: unrelated or malformed filter mail no longer passes. | Retain the exact comparison. | Resolved |
| obmgr_h01 | `expunge_obstacle_pass` | `OBM_EXPUNGE=ob_near` becomes `BHV_ABLE_FILTER=obstacle_id=ob_near,action=expunge`. | The evaluator now requires the complete expected payload through `OBM_EXPECTED_PAYLOAD`. | Resolved for the behavior the current upstream app performs; the separate removal defect is recorded under Suggested Upstream Changes. | Retain the exact translation check without making the local suite depend on an unavailable upstream fix. | Resolved |
| obmgr_h01 | `no_alert_request_absent_pass`, `bad_alert_request_absent_pass`, `given_far_absent_pass`, `invalid_given_nonconvex_absent_pass`, `post_dist_false_absent_pass`, `invalid_point_missing_key_absent_pass`, `points_ignore_range_absent_pass` | No obstacle-distance publication occurs in each expected-absence scenario. | Each evaluator now sets `OBM_DIST_SEEN=true` on any `OBM_DIST_TO_OBJ` mail and fails on that flag. | Resolved: any value now fails, rather than only one expected value. | Retain the mail-presence flag. | Resolved |
| obmgr_h01 | `given_max_duration_missing_absent_pass` | A `GIVEN_OBSTACLE` without `duration=` is rejected without producing an alert. | The evaluator now fails on either `OBM_NEW_GIVEN` or any `OBSTACLE_ALERT` mail. | Resolved: stray alert publication no longer passes. | Retain the alert-presence flag. | Resolved |
| obmgr_h02 | `lasso_cluster_pass` | `lasso=true` and `lasso_radius=8` were intended to prove pseudo-hull use during motion. | `ARRIVED=true` and `OB_TOTAL_COLLISIONS=0` were also satisfied by the normal convex hull. | The case added no lasso-specific evidence; H01 already distinguishes lasso generation and H02 `point_cluster_pass` covers behavior spawning from a generated hull. | Removed from the H02 matrix rather than retaining a false sense of coverage. | Removed |
| cmgr_h01 | `detect_strict_absent_pass`, `detect_edge_absent_pass`, `detect_far_spoof_absent_pass`, `runtime_alert_disable_absent_pass`, `filter_match_type_absent_pass` | The contact is tracked but excluded from the selected alert by range, boundary, runtime state, or type. | Each evaluator now requires `CONTACT_CLOSEST=intruder` and a case-specific range or list fact while retaining the no-alert condition. | Resolved: failure to process the contact no longer satisfies the expected-absence grade. | Retain the tracking evidence and alert-absence condition together. | Resolved |
| cmgr_h01 | `detect_short_duration_absent_pass` | A two-second spoof was intended to expire without triggering the alert. | A near-contact mutation sets `CONTACT_SEEN=true` immediately despite the short duration; the original 34-meter contact passed only because it was outside the 20-meter thresholds. | The case did not exercise a duration-specific contact-manager contract, while stale-contact removal is already covered separately. | Removed rather than retaining redundant out-of-range coverage. | Removed |
| cmgr_h01 | `post_all_ranges_pass` | `post_all_ranges=true` publishes the complete sorted range list for fixed contacts at `(20,-50)` and `(20,-40)` from ownship `(0,-60)`. | The case now disables simulator motion, seeds fixed navigation, uses stationary contacts, and requires `CONTACT_RANGES=22.4,28.3` alongside the count and list. | Resolved: missing, incomplete, unsorted, or numerically incorrect range reports fail under controlled geometry. | Retain the fixed app-level fixture and complete report comparison. | Resolved |
| cmgr_h01 | `post_closest_relbng_pass` | `post_closest_relbng=true` publishes the rounded relative bearing to `alpha` from fixed ownship heading 90 degrees. | The controlled fixture now requires `CONTACT_CLOSEST_RELBNG=333` alongside count and closest-contact identity. | Resolved: missing or incorrect bearing output fails without motion timing noise. | Retain the fixed navigation/contact geometry and numeric bearing check. | Resolved |
| cmgr_h01 | `disable_contact_pass`, `enable_contact_pass` | Named contact enable/disable mail becomes the matching `BHV_ABLE_FILTER` payload. | `CONTACT_SEEN=true`, filter mail presence, and the complete expected payload are now required. | Resolved: unrelated or malformed filter mail no longer passes. | Retain the exact public filter-string comparison. | Resolved |
| cmgr_h02 | `no_detect_clear_pass`, `filter_match_type_clear_pass`, `tight_alert_fail`, `runtime_alert_disable_fail` | A tracked contact is intentionally excluded from the avoid alert by range, type, or runtime alert state. | Each evaluator now requires `CONTACT_CLOSEST=intruder`, plus the existing no-detection, arrival, collision, and case-specific encounter evidence; the range case also grades its threshold side. | Resolved: a general contact-processing failure no longer satisfies the expected-absence grade. | Retain tracking evidence together with the no-alert condition. | Resolved |
| cmgr_h02 | `runtime_alert_reenable_pass` | A disabled static alert suppresses short-lived `early`, then alerts on `late` after the runtime enable request. | `CONTACT_RETIRED=early`, no `CMGR_ALERTED_early`, `CMGR_ALERTED_late=true`, safe arrival, and zero collisions. | Resolved: changing the disable request to enable latches the forbidden early-contact alert and fails. | Retain the two-contact phase fixture and contact-specific alert flags. | Resolved |
| cmgr_h02 | `hold_alerts_for_helm_pass` | `hold_alerts_for_helm=true` should suppress alerts until Contact Manager receives `IVPHELM_STATE=DRIVE`. | The moving stem's real helm can enter DRIVE before patch-driven contact mail, and patch overlays cannot replace its special `ANTLER` launch block to impose deterministic ordering. | The original case checked only final detection and arrival; attempted pre/post harness fixtures remained startup-order-sensitive under full logging and batch execution. | Add a direct Contact Manager CTest with ordered contact and helm-state mail; do not restore this harness case without deterministic process ordering. | Removed; CTest recommended |
| cmgr_h02 | `stale_reappear_pass` | The first `intruder` retires and the later report with the same name produces a fresh alert. | The shoreside clears `CONTACT_DETECTED` after retirement and before the second spoof; the evaluator requires it to return to true, plus retirement, safe arrival, and zero collisions. | Resolved: removing the second report leaves the reset detection flag false and fails. | Retain the explicit pre-reappearance reset. | Resolved |
| cmgr_h02 | `two_contact_pass` | `alpha` drives avoidance while off-lane `bravo` remains tracked but irrelevant. | The evaluator requires `CONTACTS_LIST=alpha,bravo` and contact-specific `CONTACT_ALERTED=alpha`, plus safe arrival and zero encounters/collisions. | Resolved: omitting `alpha` fails both the identity and complete-list evidence. | Retain exact known-contact and alert-source checks. | Resolved |
| pid_h01 | `stale_nav_pass` | Expired `NAV_SPEED` mail triggers the stale-input all-stop path while the other three inputs remain fresh. | A first phase requires nonzero rudder and thrust; the final phase requires both outputs near zero and `PID_STALE` present. | Resolved: refreshing `NAV_SPEED` before evaluation leaves nonzero outputs and fails. | Retain the isolated stale source and two-phase grade. | Resolved |
| pid_h01 | `heading_debug_saturation_pass` | `heading_debug=true` publishes `PID_HDG_DEBUG` while the rudder saturates at `maxrudder=10`. | The evaluator requires diagnostic mail, rudder in `[-10,-9]`, and thrust in `[39,41]`; the complete stable diagnostic remains in the result. | Resolved: disabling `heading_debug` leaves identical actuator output but now fails. | Retain the publication flag plus deterministic saturation inputs. | Resolved |
| pid_h01 | `output_suffix_alt_pass` | `output_suffix=_ALT` moves actuator publications exclusively to `DESIRED_RUDDER_ALT` and `DESIRED_THRUST_ALT`. | Expected suffixed values are required and mail flags fail on either unsuffixed actuator name. | Resolved: forcing the forbidden flag makes the otherwise correct case fail. | Retain the positive and negative publication evidence together. | Resolved |
| pid_h01 | `override_release_needs_fresh_mail_pass` | Inputs received during override must not become actionable merely because override is released. | Three ordered phases require initial nonzero control, zero output with control false during override, then zero output with control true after release without fresh mail. | Resolved: suppressing the override event fails during the middle phase. | Retain the ordered state checks. | Resolved |
| pid_h01 | `override_recover_fresh_mail_pass` | Fresh helm/nav mail after override release restores control. | Ordered phases require initial port control, zero output during override, and starboard control only after fresh post-release mail. | Resolved: omitting the four fresh inputs leaves zero output and fails the recovery phase. | Retain the ordered state checks. | Resolved |
| pid_h01 | `stale_recover_pass` | The PID enters stale all-stop and resumes control only after all helm/nav inputs are refreshed. | Ordered phases require initial port control, zero output with stale evidence, and recovered starboard control after refresh. | Resolved: refreshing all inputs before the stale checkpoint keeps control active and fails. | Retain the ordered stale and recovery checks. | Resolved |
| pid_h02 | `rudder_bias_recover_pass` | `sim_instability=18` perturbs rudder control and the closed loop rejects the disturbance. | At four seconds the initially straight transit must have `ABE_DESIRED_RUDDER<-1`; the final phase retains arrival and the widened motion corridor. | Resolved: setting `sim_instability=0` leaves rudder at zero at the checkpoint and fails. | Retain the early disturbance checkpoint and final recovery grade. | Resolved |
| pid_h02 | `override_release_pass` | The vehicle remains stopped under startup override, then reacquires control after release at eight seconds. | Before release, override must still be true with x in `[-0.25,0.25]` and speed at most `0.1`; the final phase requires arrival in the normal corridor. | Resolved: starting with override false moves the vehicle before the checkpoint and fails. | Retain both phases. | Resolved |
| pid_h02 | `low_elevator_authority_fail` | `maxelevator=0.25` limits, but does not eliminate, the commanded dive toward 30 meters. | At 45 seconds the vehicle must be between 10 and 25 meters deep with elevator in `[0.2,0.3]`, alongside the forward-track conditions. | Resolved: removing the elevator cap reaches roughly 30 meters with a non-capped final command and fails. | Retain the broad partial-depth band and near-limit actuator band. | Resolved |
| usim_marine_h01 | `start_pos_seed_pass` | Every configured `start_pos` field, including `depth=3`, seeds the initial simulator state. | Final x/y, heading, speed, and a new depth band of `[2.8,3.2]` are required after southbound motion with zero buoyancy. | Resolved: changing the configured depth to zero leaves planar motion unchanged but fails. | Retain the complete five-field grade. | Resolved |
| usim_marine_h01 | `disabled_nav_seed_pass` | While `USM_ENABLED=false`, incoming `NAV_X`, `NAV_Y`, and `NAV_HEADING` seed simulator state for later use. | The case reenables the simulator, applies thrust, and requires simulator-owned westward motion from the seeded pose with speed in `[0.8,1.2]`. | Resolved: keeping the simulator disabled leaves the injected pose and zero speed, which fails. | Retain the disable, seed, reenable, and motion sequence. | Resolved |
| usim_marine_h01 | `dual_state_reset_nav_pass` | Drift first separates navigation from ground truth, then `USM_RESET_NAV` resynchronizes them. | A pre-reset phase requires `NAV_X` near zero and `NAV_GT_X>=3`; after reset and drift shutdown, both x states must be in `[3,8]`. | Resolved: omitting the reset leaves ordinary nav at zero and fails the final phase. | Retain both phases and the post-reset drift shutdown. | Resolved |
| pnodereporter_h01 | `platform_report_gap_pass` | `gap=2` delays publication of a changed battery value until the configured interval expires. | The retained platform-report history requires `batt=12.7` followed 1.8–2.4 seconds later by `batt=12.8`. | Resolved: changing the configured gap to zero produces the same final payload but fails the history contract. | Retain both payloads and their broad two-second interval. | Resolved |
| pnodereporter_h01 | `coord_policy_global_pass` | Global-only policy suppresses every local `X` and `Y` field. | Parsed latitude/longitude must match the supplied global pair within `0.000001` degrees, and the payload must contain no `X` or `Y` component. | Resolved: any local coordinate value now fails. | Retain parsed component checks rather than substring-specific exclusions. | Resolved |
| pnodereporter_h01 | `crossfill_local_to_global_pass`, `crossfill_global_to_local_pass`, `crossfill_global_terse_pass`, `crossfill_fill_empty_pass`, `crossfill_use_latest_local_pass` | Each cross-fill policy computes the correct counterpart coordinates and applies its source-selection rule. | Every retained and generated coordinate is parsed and compared with a policy-specific meter- or microdegree-scale band; global-terse must replace conflicting `(99,99)`. | Resolved: incorrect conversion, missing counterpart fields, or the wrong authoritative source fails. | Retain policy-specific numeric bands and conflicting inputs. | Resolved |
| pnodereporter_h01 | `mhash_odometer_pass` | Moving from x `0` to `20` updates the mission-hash odometer extension to the traveled distance. | `PNR_MHASH` must contain both `mhash=` and exact `ext=20`. | Resolved: presence without the measured extension no longer passes. | Retain the straight 20-meter displacement and exact serialized extension. | Resolved |
| pnodereporter_h01 | `report_cog_pass` | Motion from `(0,0)` to `(8,0)` produces course over ground 90 degrees. | Report history must contain the moved position with `COG=90`; the later stationary `COG=0` snapshot is intentionally ignored. | Resolved: disabling COG calculation preserves final position but fails the history contract. | Retain the transient moved-report observation. | Resolved |
| pnodereporter_h01 | `pause_resume_pass` | Nav updates are absorbed while paused but no report is published until resume. | `(1,1)` is posted first, `(30,-10)` replaces it while paused, and the first report after resume must contain `(30,-10)` with no `(1,1)` report anywhere. | Resolved: `paused=false` publishes the initial state and fails; moving the replacement state before resume also removes the original scheduler race. | Retain the update-before-resume ordering and full report-history check. | Resolved |
| pnodereporter_h01 | `extrap_gap_metric_pass` | The jump from predicted eastbound motion to observed `X=3` produces the expected position discrepancy without a heading discrepancy. | History must contain a position gap in `[2.4,2.9]` and a heading gap in `[0,0.01]`. | Resolved: changing the update to `X=2` leaves final nonnegative metrics but fails the transient discrepancy band. | Retain the history check rather than the incidental final loop gap. | Resolved |
| pnodereporter_h01 | `extrap_prune_pass` | The `NAV_X=12` update is measured against a capped prediction of `5` and pruned below the ten-meter threshold. | Final position gap must be `[6.5,7.5]`, heading gap below `0.1`, and the report must remain at `(0,0)` without `X=12`. | Resolved: ignoring the update cannot produce the required seven-meter discrepancy. | Retain both processed-update evidence and report absence. | Resolved |
| upokedb_h01 | `quiet_mode_pass` | A long-form `--quiet` invocation posts successfully. | The ordinary `numeric_direct_pass` command already uses `--quiet` and requires its exact numeric publication; `quiet_short_alias_pass` separately covers `-q`. | The dedicated case duplicated the default command path without distinct evidence. | Removed while retaining both long- and short-option coverage in discriminating cases. | Removed |
| upokedb_h01 | `overwrite_existing_pass`, `repeated_batch_pass` | A later invocation replaces an earlier value for the same variable. | `overwrite_existing_pass` retains two forced-string commands separated by 0.5 seconds and requires the second value. | Resolved: replacing the second `new` posting with another `old` posting produces a mission-grade failure; the removed case implemented the same contract with only variable/value names changed. | Retain the single explicit overwrite case. | Resolved; duplicate removed |
| upokedb_h01 | `multiple_mixed_pass`, `mixed_numeric_string_batch_pass` | One invocation accepts numeric and forced-string arguments together. | `mixed_numeric_string_batch_pass` requires numeric `31` and the exact pMissionEval result field `mixed_str=031`, proving both mixed types and leading-zero preservation despite LogicCondition's numeric coercion. | Resolved: changing `:=031` to `=031` leaves the mission grade green but changes the result field to `31`, which the harness supplement rejects; the removed broader case contributed no additional parser path. | Retain the leading-zero mixed case and its narrow structured-result assertion. | Resolved; duplicate removed |
| upokedb_h01 | `time_macros_alias_pass` | Source-defined aliases `@UTC` and `@MOOSTIME` both expand to MOOS-time values in one invocation. | Four postings cover both spellings and must each fall in the broad `10^9` to `10^11` MOOS-time range. | Resolved: unchanged tokens and arbitrary small positive constants fail, while the wide bound avoids coupling the case to scheduling order or a narrow wall-clock window. | Retain the combined source-backed alias case and timing-independent bounds. | Resolved; replaces two weak cases |
| upokedb_h01 | `cache_duplicate_first_wins_pass` | Duplicate cached `poke=` entries have a defined precedence contract. | Local source appends cache entries in the order returned by `MissionReader`, but neither uPokeDB source comments nor interface help defines duplicate-name precedence. | Treating the observed first-wins result as normative would lock an incidental configuration-order detail. | Removed; restore only if upstream documents a duplicate-cache precedence contract. | Removed |
| uxms_h01 | `show_time_only_pass`, `show_time_community_pass`, `shortcut_source_time_pass` | `--show=time` and `-st` add a populated numeric time column, alone or beside community/source columns. | Each case now requires a complete `XMS_ALPHA` row with a numeric timestamp in the option-specific column order. | Resolved: removing `--show=time` leaves the old header token and value evidence intact but fails `time_value`. | Retain row-level regular expressions rather than header substrings. | Resolved |
| uxms_h01 | `history_var_pass` | `--history=XMS_HIST` collects and renders distinct `first`, `second`, and `third` publications oldest-to-newest. | The publisher events now occur after uXMS connects, and the capture must contain all three quoted values in order. | Resolved: removing the `second` publication produces a mission-grade pass but fails `history_sequence`. | Retain post-connect stimulus timing and ordered terminal-output grading. | Resolved |
| uxms_h01 | `clean_cli_scope_pass`, `clean_shortcut_scope_pass` | `--clean` and `-c` discard both mission-file variables before applying the CLI `XMS_NUM` scope. | Both cases require `XMS_NUM=42` evidence and forbid `XMS_ALPHA` and `XMS_SECONDARY`. | Resolved: removing long `--clean` while leaving only `XMS_SECONDARY` in file scope fails specifically on the secondary variable. | Retain both configured-variable exclusions for each spelling. | Resolved |
| uxms_h01 | `colorany_pass`, `colormap_pair_pass` | `--colorany=XMS_ALPHA` assigns automatic blue and `--colormap=XMS_ALPHA,red` assigns explicit red to the matching row. | Each case requires the option-specific ANSI sequence immediately around `XMS_ALPHA`, plus the normal value evidence. | Resolved: removing either color option leaves the plain row intact but removes the required escape sequence. | Retain exact ANSI checks because terminal coloring is the public output contract. | Resolved |
| uxms_h01 | `events_mode_pass` | An explicit `--mode=events` selects uXMS's existing default refresh mode. | The removed case supplied the default option and checked only ordinary row output. | No output observation can distinguish honoring the option from ignoring it when both select the same default state. | Removed as redundant; ordinary event-mode startup remains exercised implicitly across default-mode cases. | Removed |
| uxms_h01 | `paused_shortcut_pass` | `-p` selects paused mode and prevents a later `XMS_PAUSE=later` post from refreshing the displayed `initial` value. | The capture must contain the `PAUSED` label and `initial`, while forbidding `later`. | Resolved: removing `-p` switches to event mode, displays `later`, and lacks the required mode label. | Retain the post-connect update and absence check. | Resolved |
| uxms_h01 | `alll_mode_pass` | `--alll` includes `APPCAST`, which ordinary `--all` deliberately excludes. | The case requires a row beginning with `APPCAST` alongside ordinary all-scope evidence. | Resolved: changing `--alll` to `--all` removes the APPCAST row and fails `appcast_row`. | Retain row-level matching so incidental `APPCAST` text inside summary values cannot satisfy the case. | Resolved |
| uxms_h01 | `alias_proc_name_pass` | `--alias=uXMS_ci` registers the viewer under the requested MOOS client name. | The case scopes `DB_CLIENTS` and requires its row to contain `uXMS_ci`, while also retaining the normal `XMS_ALPHA` row. | Resolved: removing the alias registers a generated `uXMS_<number>` name and fails `alias_client_row`. | Retain the independent MOOSDB client-list observation. | Resolved |
| uquerydb_h01 | `late_var_wait_pass` | `--wait=3` keeps the query active until `QUERY_LATE=arrived` is posted at mission time 1.5. | The launcher waits 0.75 real seconds after starting a time-warp-10 mission before invoking `uQueryDB`, then requires exit code `0`. | At the normal warp, the nominally late value is already in MOOSDB before the query starts, so the case does not exercise waiting for new mail. | Start `uQueryDB` before the delayed post or move the post beyond the launcher delay, then record elapsed time or another pre-post running-state observation. | Open |
| uquerydb_h01 | `missing_var_timeout_fail`, `config_halt_max_time_timeout_fail` | `--wait=1` and `halt_max_time=1` terminate an unsatisfied query at the configured one-second boundary. | Each case requires exit code `1`; the runner only imposes a separate eight-second safety deadline. | Immediate failure or any exit before eight seconds satisfies the verdict, so the configured duration and alias equivalence are not checked. | Measure subject elapsed time and require a tight band around one second for both CLI and config forms. | Open |
| uquerydb_h01 | `multiple_pass_conditions_pass`, `compound_and_pass` | Separate pass conditions and a compound `and` both require every term to be true. | Both terms are true and the only subject evidence is exit code `0`. | Ignoring the second condition or interpreting AND as OR can still pass both cases. | Add a companion case for each syntax with exactly one false term and require exit code `1`. | Open |
| uquerydb_h01 | `checkvar_esv_pass`, `checkvar_csv_pass`, `checkvar_wsv_pass`, `checkvar_value_only_pass`, `checkvar_multi_csv_pass`, `checkvar_timeout_esv_pass`, `case_config_checkvar_csv_pass`, `mission_file_config_pass` | `.checkvars` contains exactly the requested variables in the selected format, with one row per variable and no extra fields. | The harness greps required substrings; only the value-only case forbids three common prefixes. | Extra rows, duplicate rows, reordered fields, or surrounding junk can pass the substring checks. | Parse `.checkvars` by exact lines and require the complete expected file for each format, including row count and order where contractual. | Open |
| uquerydb_h01 | `unique_name_pass` | `--unique` registers a randomized `uQueryDB_<number>` client name. | A successful `QUERY_NUM=42` query and exit code `0`. | The same query passes if `--unique` is ignored and the ordinary `uQueryDB` name is used. | Observe `DB_CLIENTS` during the query and require a `uQueryDB_<number>` name while rejecting the plain name for that client. | Open |
| pechovar_h01 | `latest_only_repeated_source_pass` | `echo_latest_only=true` collapses the queued `first` and `second` source messages into one target publication containing `second`. | pMissionEval only requires final `ECHO_OUT_LATEST=second`. | The default two-publication behavior reaches the same final value, so ignoring `echo_latest_only` passes. | Count `ECHO_OUT_LATEST` before evaluation and require exactly one publication, complementing the default-mode case's count of two. | Open |
| pechovar_h01 | `flip_explicit_param_syntax_pass` | Explicit `filter =` and `component =` entries both parse and control the EFlipper. | Matching input produces `xcenter=18`. | The result proves the explicit component entry parsed, but output is unchanged if the explicit matching filter is ignored. | Add an explicit-syntax case with a mismatched filter and require no destination publication. | Open |
| pechovar_h01 | `flip_unmapped_component_omitted_pass` | Only mapped `x` is serialized from `x=8,z=99`; unmapped `z` is absent from the `FLIP_OUT` value. | Requires `xcenter=8` and forbids only the combined substring `xcenter=8,z=99` anywhere in the logs. | `FLIP_OUT=xcenter=8,z=99` with different spacing/order, or another unwanted serialization, can pass. | Extract the `FLIP_OUT` publication value and compare it exactly with `xcenter=8`. | Open |
| pechovar_h01 | `flip_empty_value_pass` | Mapping `x=` preserves an empty value as exact destination payload `xcenter=`. | The logs need only contain the substring `xcenter=`. | Any nonempty value such as `xcenter=wrong` contains the required substring and passes. | Extract `FLIP_OUT` and require its complete value to equal `xcenter=`. | Open |
| pechovar_h01 | `invalid_flip_alias_no_output_pass` | An EFlipper with identical source and destination variables is rejected without making any additional `FLIP_SRC` publication. | Only the mapped token `xcenter=17` is forbidden. | A bad implementation that reposts the original `x=17` or another payload to `FLIP_SRC` can pass. | Count `FLIP_SRC` and require exactly the one uTimerScript source post, as in the self-cycle case. | Open |
| pechovar_h01 | `shared_source_dual_flip_pass`, `dual_flip_independent_pass` | Each expected mapped payload is published on its configured destination variable. | Required payload substrings are searched across all alog lines, without binding each token to a variable. | Swapping the destination variables or publishing both payloads on one destination can satisfy the current checks. | Query each destination variable separately and compare its exact payload before the evaluation cutoff. | Open |
| utimerscript_h01 | `macro_count_nav_pass` | `NAV_X`, `NAV_Y`, `COUNT`, and `TCOUNT` all expand into the expected structured payload. | The alog check requires only the `x=12.50,y=-4.25` prefix; count fields are reported but not graded. | Missing or incorrect `count` and `tcount` expansions pass. | Compare the complete `UTS_MACRO` value with `x=12.50,y=-4.25,count=2,tcount=2`. | Open |
| utimerscript_h01 | `condition_gate_external_pass`, `pause_unpause_external_pass`, `pause_toggle_external_pass`, `block_on_peer_pass`, `multi_condition_gate_pass` | Each gate, pause, peer block, or second condition prevents its event from publishing until the releasing transition. | Only final output and final control state are graded. | Ignoring the blocking interval and publishing early reaches the same final state. | Add pre-release absence flags or publication timestamps, then retain the final presence checks after release. | Open |
| utimerscript_h01 | `forward_jump_next_pass`, `forward_positive_skip_pass` | `UTS_FORWARD=0` jumps to the next event and a positive value advances script time enough to release a future event early. | Both cases only require the scheduled value eventually; their evaluation windows also allow natural publication. | Ignoring forward mail passes, and in the positive case the poke normally arrives after the time-5 event at warp 10. | Place events beyond the natural evaluation horizon, deliver forward before they are due, and grade publication time or pre-forward absence/post-forward presence. | Open |
| utimerscript_h01 | `delay_start_pass`, `time_warp_accelerates_pass`, `delay_reset_pass`, `time_window_event_pass` | Start delay, app time warp, reset delay, and event windows alter when publications occur. | Final values and configuration-status substrings are graded, plus a minimum repost count for delay reset. | The timing modifiers can be ignored while all cases still reach the same final values/status configuration. | Capture publication timestamps and require case-specific timing bands, ideally against an unmodified control. | Open |
| utimerscript_h01 | `randvar_uniform_bounds_pass`, `randvar_gaussian_bounds_pass` | Uniform and gaussian generators follow their configured distributions rather than merely returning an in-range value. | One sample per case is checked only against min/max bounds. | A constant or the wrong distribution passes. | Use deterministic seeding if available and compare expected samples, or collect enough samples for distribution-specific mean/spread checks while retaining hard bounds. | Open |
| utimerscript_h01 | `randpair_poly_bounds_pass` | `type=poly` samples inside an arbitrary polygon. | The fixture polygon is the axis-aligned square `[0,10] x [0,10]`, and only that bounding box is graded. | Independent rectangular sampling passes without exercising polygon containment. | Use a non-rectangular polygon and test each sampled point with point-in-polygon containment. | Open |
| utimerscript_h01 | `custom_control_vars_pass` | All three rebound names independently control pause, forward, and reset behavior. | Final `TCOUNT=1` and `resets=1/any` primarily prove the custom reset name; no paused interval or forward-induced timing is graded. | Default or ignored pause/forward handling can pass if the custom reset works. | Split the controls into focused cases or add phase-specific absence, release, early-post, and repost evidence for each custom variable. | Open |
| utimerscript_h01 | `reset_var_repost_pass`, `delay_reset_pass` | Each reset path produces exactly one second publication before evaluation. | The alog `count:` check accepts any count greater than or equal to two. | Runaway or duplicate extra posts pass. | Support an exact-count check and require exactly two publications. | Open |
| utimerscript_h01 | `dbtime_utc_macro_pass` | `DBTIME` and `UTC` expand into their correct time domains at the event timestamp. | Both values only need to be positive. | Arbitrary positive constants or identical handling of distinct clock semantics pass. | Capture expected before/after windows for each clock and compare their relationship at the same post. | Open |
| utimerscript_h01 | `shuffle_false_status_pass` | `shuffle=false` preserves declared order for same-time or otherwise reorderable events. | A single payload publishes and status contains the configured text `shuffle=false`. | The case proves configuration reporting but provides no ordering decision for shuffle to affect. | Schedule several same-time indexed events, capture their order, and compare it with declaration order. | Open |
| utimerscript_h01 | `quit_event_pass` | The `quit` event terminates uTimerScript after its readiness publication. | pMissionEval grades only `UTS_QUIT_READY=true`; the harness does not inspect the process after the quit time. | Ignoring the quit event passes. | Observe `DB_CLIENTS`, process exit status, or a post-quit health check proving uTimerScript is gone while the mission remains evaluable. | Open |
| pdeadmanpost_h01 | `stop_heartbeat_posts_pass` | Heartbeats at 0.2 and 0.4 refresh the watchdog, which trips only after the later heartbeat becomes stale. | The final `DEAD_STOP=true` value is required by time 1.2. | Ignoring both heartbeats and tripping from startup at 0.45 reaches the same final state. | Grade the deadflag publication timestamp after the last heartbeat, or require pre-0.85 absence followed by post-0.85 presence. | Open |
| pdeadmanpost_h01 | `reset_policy_reposts_after_heartbeat_pass` | `post_policy=reset` produces one initial post and exactly one additional post after the heartbeat resets the latch. | The harness requires at least two `DEAD_RESET` publications. | Repeat-style runaway posting also satisfies the minimum count. | Add `MAX_COUNTS=("DEAD_RESET:2")` so the case requires exactly two publications. | Open |
| pdeadmanpost_h01 | `zero_max_noheart_posts_pass` | `max_noheart=0` trips immediately rather than after an ordinary positive timeout. | One `DEAD_ZERO_MAX` publication is required by time 0.8. | Any timeout below 0.8, including the default 0.5, satisfies the verdict. | Compare the publication timestamp with a tight near-start bound and a positive-timeout control. | Open |
| pspoofnode_h01 | `config_static_report_pass`, `mail_spoof_report_pass`, `mail_group_vsource_pass`, `negative_speed_accepted_pass` | Explicit request fields, including position and speed, survive parsing into the same structured node report. | Each case searches selected field tokens across report history; several supplied fields such as X/Y and zero speed are not required. | A report with incorrect omitted fields can pass, and token searches do not require one complete expected row. | Parse a named contact's report and compare every explicitly supplied field in one report, with numeric tolerances where movement applies. | Open |
| pspoofnode_h01 | `default_fields_pass`, `runtime_defaults_pass` | Every supported configured default has the documented inheritance behavior. | Type, color, heading, speed, and length are required; configured `default_group` and `default_vsource` are not graded. | Incorrect group/source inheritance passes both cases. | Require the intended `GROUP` and `VSOURCE` values, or remove those defaults if their deliberate non-inheritance is already covered separately. | Open |
| pspoofnode_h01 | `generated_name_pass` | An unnamed contact receives a valid generated name in the documented format. | Report history only needs a `NAME=C` substring. | A constant `C`, malformed suffix, or non-unique generated name passes. | Create at least two unnamed contacts and require distinct names matching the exact generated-name pattern. | Open |
| pspoofnode_h01 | `cancel_vname_pass`, `cancel_group_pass`, `cancel_contact_alias_pass` | Cancellation removes only the targeted contact or group while an untargeted peer continues reporting. | Both contacts must appear before cancellation, and the targeted name must be absent afterward. | The untargeted peer may also disappear after cancellation without failing. | Require a post-cancel report for `golf`, `india`, or `lima` respectively, in the same window used for target absence. | Open |
| utermcommand_h01 | `numeric_command_pass`, `negative_numeric_command_pass` | Numeric-looking command payloads are published as double-valued MOOS mail rather than numeric strings. | pMissionEval compares the final values with numeric literals. | Depending on LogicBuffer coercion, a string `"42"` or `"-3.5"` may satisfy the same equality and leave message type untested. | Inspect the alog type/source columns or add a type-aware observer that requires double mail. | Open |
| utermcommand_h01 | `multiple_commands_pass` | One running uTermCommand session accepts two successive keys and posts both configured commands. | The launcher starts a fresh uTermCommand process for `aa`, exits it, then starts another for `bb`; final MOOS values are graded. | State or input-loop failures after the first command in one session are not covered. | Feed `aa\nbb\n` through a single terminal session and require both publications before quitting. | Open |
| psearchgrid_h01 | `reset_grid_clears_pass`, `reset_false_payload_clears_pass` | Reset mail publishes a cleared full-grid state with cell 0 restored to `x:0`, for both true and false payloads. | The incremented `cell=0:x:1` token must disappear after reset. | Stopping all full-grid publication after reset satisfies the absence check without proving cleared state was emitted. | Require a post-reset `VIEW_GRID` publication containing exact `cell=0:x:0` and no nonzero cell value. | Open |
| psearchgrid_h01 | `two_cell_delta_pass` | Two simultaneous reports in different cells produce increments for both cell 0 and cell 1. | Delta history only needs one generic `,x,1` token. | Processing either report alone, or assigning both to one cell, passes. | Parse the delta payload and require distinct cell identifiers for both expected `x,1` increments. | Open |
| depth_constant_h01 | `constant_depth_shape_params_pass`, `constant_depth_summitdelta_clip_pass` | The configured peak width, base width, and summit delta are applied to the depth ZAIC, including clipping `summitdelta=250` to its supported range. | Each case grades transit progress, final depth and mismatch bands, and absence of behavior errors. | Ignoring the supplied shape parameters and using another reasonable ZAIC can reach the same target depth and pass. | Expose or inspect the constructed ZAIC parameters, or add a trajectory/control-output comparison that discriminates the requested shape and clipped value from the defaults. | Open |
| depth_constant_h01 | `constant_depth_duration_complete_pass` | A finite eight-second constant-depth behavior completes cleanly and posts `CD_DONE=true`. | At 22 seconds, only `CD_DONE=true` and `NAV_X>=20` are required; the recorded representative result has `bhv_error=true`. | The case passes despite a behavior error and does not establish clean completion. | Determine the source of the post-completion error, then require `BHV_ERROR_SEEN=false` if clean completion is intended. | Open |
| depth_constant_h01 | `constant_depth_low_elevator_authority_fail` | `maxelevator=0.05` bounds the commanded elevator while preventing the vehicle from reaching a 24-meter target. | The evaluator requires `DESIRED_ELEVATOR<=0.05` and `NAV_DEPTH<=22`. | The elevator check is one-sided, so any negative value below `-0.05` also passes and the configured magnitude limit is not proved. | Require `-0.05<=DESIRED_ELEVATOR<=0.05`, and preferably require a near-saturation command while the depth error remains large. | Open |
| depth_goto_h02 | `goto_depth_bad_repeat_fail` | A genuinely invalid `repeat` value is rejected during behavior configuration. | The fixture sets `repeat=0`, which `BHV_GoToDepth::setParam` explicitly accepts, alongside the otherwise valid single level `depth=5,1`; the stored result nevertheless reports `HELM_MALCONFIG`. | The fixture does not contain the invalid repeat value claimed by the case name, and the recorded verdict is not explained by the intended fault. | Change the fixture to an unambiguously invalid value such as `repeat=-1`, rerun it, and separately investigate why the current valid-looking fixture produced `HELM_MALCONFIG`. | Open |
| depth_goto_h02 | `goto_depth_sequence_pass`, `goto_depth_repeat_pass`, `goto_depth_crossing_pass`, `goto_depth_zero_delta_crossing_pass`, `goto_depth_repeat_exhaustion_pass` | Normal sequence traversal, crossings, and repeat completion occur without behavior errors. | The evaluators grade arrival counts, position/depth, and sometimes completion, but do not require `BHV_ERROR_SEEN=false`; representative results record `bhv_error=true` for all five cases. | Functional counters can pass while the behavior reports an error, concealing a lifecycle or post-completion defect. | Identify the emitted error and require `BHV_ERROR_SEEN=false` if it is not an explicitly accepted completion artifact. | Open |
| depth_goto_h02 | `goto_depth_repeat_pass` | `repeat=1` produces exactly two traversals of the two-level sequence and then stops. | The evaluator requires only `GTD_LEVEL_HIT>=4` and `NAV_X>=70`; unlike the exhaustion case, it does not grade the completion flag or an upper count bound. | Extra traversals or runaway arrival posts still pass. | Require `GTD_LEVEL_HIT=4` and a completion endflag, or remove this weaker duplicate in favor of `goto_depth_repeat_exhaustion_pass`. | Open |
| depth_periodic_surface_h03 | `periodic_surface_quadratic_ascent_pass`, `periodic_surface_fullspeed_ascent_pass`, `periodic_surface_current_speed_pass`, `periodic_surface_speed_alias_pass` | Each ascent grade and speed mode produces its distinct commanded-speed profile during ascent. | Every case grades only that an ascent and later surface timeout occurred, with no behavior error. | Ignoring the selected grade or speed and using any successful ascent profile passes. | Record `DESIRED_SPEED` and depth over the ascent, then compare the case-specific profile, including preservation of the current speed and the `speed_to_surface` value. | Open |
| depth_periodic_surface_h03 | `periodic_surface_status_vars_pass`, `periodic_surface_status_variable_alias_pass` | Short and long parameter spellings publish meaningful pending and at-surface counters on the requested variable names. | The requested variables must exist, but pending uses only an upper bound (`<=8`, allowing any negative value) and dwell uses only `>=0`. | Incorrect counter values or semantics can pass once mail appears on the custom names. | Grade bounded, phase-specific counter values before ascent and during surface dwell, including monotonic direction and reset behavior. | Open |
| depth_periodic_surface_h03 | `periodic_surface_bad_acomms_mark_fail`, `periodic_surface_bad_acomms_max_fail` | Separate malformed acoustic-mark tuple rules are covered. | Both cases use the identical `ACOMMS_MARK,20,10` fixture and require the identical `HELM_MALCONFIG` outcome. | The two names imply different faults but provide duplicate coverage. | Keep one interval-greater-than-maximum case and change the other to a genuinely distinct malformed tuple, such as wrong arity or an invalid variable token. | Open |
| depth_max_h04 | `max_depth_tight_tolerance_pass`, `max_depth_zero_tolerance_pass` | One-meter and zero-meter tolerances constrain the guard more tightly than the ordinary four-meter setting. | The evaluators allow depth and slack to extend two meters past the configured guard in both cases. | The grading bands are wider than the distinguishing tolerance values, so behavior equivalent to a two-meter tolerance passes. | Tighten depth/slack bounds to reflect each configured tolerance and compare against a wider-tolerance control or mutation. | Open |
| depth_max_h04 | `max_depth_basewidth_alias_pass` | `basewidth=4` is interpreted identically to `tolerance=4`, rather than ignored or accepted as a no-op. | The evaluator duplicates the ordinary guard's final depth and slack bands. | A default or ignored alias that still constrains the vehicle inside those broad bands can pass. | Compare the alias case with a nondefault discriminating width and require matching trajectory/slack evidence from the canonical `tolerance` case. | Open |
| depth_min_altitude_h05 | `min_altitude_noncritical_missing_altitude_fail` | With altitude unavailable, `missing_altitude_critical=false` exercises the behavior's documented noncritical fallback. | The actual behavior fixture sets `missing_altitude_critical=true`; its simulator and evaluator duplicate `min_altitude_missing_nav_fail`. | The named noncritical missing-altitude path is never entered. | Set `missing_altitude_critical=false`, provide `NAV_DEPTH` while withholding only `NAV_ALTITUDE` if possible, and grade the intended fallback rather than generic absence of all navigation mail. | Open |
| depth_min_altitude_h05 | `min_altitude_low_threshold_pass` | A one-meter minimum clearance keeps the vehicle above the bottom rather than permitting bottom penetration. | The evaluator requires only `NAV_ALTITUDE<=4`, with no lower altitude bound. | Zero or negative altitude satisfies the current verdict. | Require `NAV_ALTITUDE>=1` within a suitable dynamics tolerance, or at minimum require a positive lower bound while retaining the near-bottom upper bound. | Open |
| colregs_h01 | All 22 classification cases | The expected COLREGS classification leads to a collision-free encounter outcome. | pMissionEval grades immediately when the expected index or mode first appears, requiring only that `COLLISION_TOTAL=0` at that early point and timeout has not occurred; representative rows have `encounters=0` and `arrived=false`. | A correct transient classification followed by a collision still passes, so the cases prove classification selection but not collision avoidance. | Separate classification and execution claims: retain the early classification latch, then continue until encounter completion or all-clear and require zero collisions (and an appropriate CPA/encounter result) before grading. | Open |
| colregs_h02 | All 56 default and 2 exploratory threshold cases | Each named below/edge/above fixture isolates the stated source threshold and demonstrates the expected side of that boundary. | Most evaluators require only the expected classification; only the turn-gap family also grades the underlying `XCN_BOW_DIST_BEN` and `XCN_BOW_TURN_GAP_BEN` values. | The other cases do not prove that the named threshold quantity is actually below, at, or above its cutoff, so a classification change caused by another geometric predicate can pass. | Report and grade the source predicate used by each family—relative heading, bearing band, outer distance, range, or CPA—alongside the expected mode transition. | Open |
| colregs_h02 | `giveway_turngap_below_pass`, `giveway_turngap_edge_pass`, `giveway_turngap_above_pass` and mirrors | The triplet isolates the ten-meter turn-gap split. | Between cases, both vessel positions and one or both speeds change; the evaluator grades the resulting bow distance and turn gap. | The classification flip cannot be attributed to one controlled input, and other derived geometry changes with the turn gap. | Hold all but one fixture input constant if possible, or document and grade the full derived-geometry vector proving only the intended source predicate crosses its cutoff. | Open |
| colregs_h02 | All 58 exposed cases | Threshold classification remains correct through the encounter and does not lead to collision. | As in H01, nearly every evaluator grades immediately when the target mode/index first appears; representative rows show `encounters=0` and `arrived=false`. | Later reclassification or collision is outside the verdict. | If execution safety is intended, latch the threshold classification then wait for all-clear/encounter completion before checking collision and CPA outcomes. | Open |
| colregs_h03 | `crossing_port_standon_unsure_bow_execution_pass`, `crossing_port_standon_close_unsure_bow_execution_pass`, `crossing_port_standon_unsure_execution_pass` | The named transient stand-on classification occurs and then produces the graded completion outcome. | Each evaluator grades only arrival, CPA, final side relations, no crossing, timeout, and collisions; it never latches the named `unsure_bow` or `unsure` mode. | The case passes even if the behavior never enters the classification that distinguishes it from its sibling. | Latch the required transient index (36 or 38) before accepting `ARRIVED_ben`, then retain the existing completion envelope. | Open |
| colregs_h03 | `overtaking_starboard_execution_pass` | The compact overtaking maneuver finishes with the intended fore/aft relationship as well as passing side. | Unlike the other overtaking evaluators, this case does not grade `CN_FORE_OF_OS_BEN`; the result records `cn_fore=1`. | A regression that leaves Ben aft at completion still passes. | Add `CN_FORE_OF_OS_BEN=1` if repeated runs confirm it is stable, or state why the compact geometry cannot support that assertion. | Open |
| colregs_h04 | `eval_tol_default_pass`, `eval_tol_long_pass` | The default and 240-second evaluation horizons retain overtaking-starboard classification while producing their expected execution envelopes. | Both evaluators grade arrival, collision, timeout, CPA, relative side/fore position, and track crossing, but neither requires the overtaking-starboard index 43. | Either case can pass without entering the classification that distinguishes the fixture from another safe encounter. | Latch `COLREGS_AVOID_IX_BEN=43` as the short-tolerance case already does, then retain the existing completion criteria. | Open |
| colregs_h04 | `pwt_grade_quadratic_pass` | The quadratic relevance curve produces the expected weight at the evaluator's 30-to-33-meter range gate. | The current evaluator requires range 30 through 33 and priority weight 15 through 30; the committed representative result records range 34.156 and weight 12.807. | The checked-in result is inconsistent with the current evaluator and cannot substantiate the documented verdict. | Rerun the focused case, then either update the stale result or reconcile the evaluator bounds with the intended quadratic curve. | Open |
| colregs_h04 | `check_plateaus_true_pass`, `refinery_on_pass` | The two cases independently cover plateau checking and refinery enablement. | They use the same geometry, the same `use_refinery=true,check_plateaus=true` behavior overlay, and equivalent evaluators requiring index 50 plus the same plateau/basin/logic signature. | The cases are duplicates and provide no independent parameter coverage. | Give `check_plateaus_true_pass` a distinct control comparison or remove one duplicate after confirming no separate contract is intended. | Open |
| collision_h01 | `post_per_contact_info_pass` | `post_per_contact_info=true` publishes the correct per-contact range and closing-speed values. | The evaluator requires only that `RANGE_AVD_intruder` and `CLSG_SPD_AVD_intruder` each publish at least once. | Missing, stale, or numerically incorrect values can pass. | Parse both publications and compare them with geometry-derived numeric bands at a defined checkpoint. | Open |
| collision_h01 | `pwt_grade_quadratic_pass`, `pwt_grade_quasi_pass`, `use_refinery_pass` | Each alternate objective-function construction path is entered and produces a valid, meaningfully distinct IvP function. | All three use the baseline evaluator: contact received, lifecycle ended, contact resolved, arrival, and zero collisions. | Ignoring the changed parameter and running the stock linear, unrefined behavior can satisfy every verdict. | Grade a branch-specific diagnostic, objective-function property, or discriminating trajectory/priority-weight envelope, then confirm with a parameter-ignored mutation. | Open |
| collision_h01 | `no_extrapolate_pass` | `extrapolate=false` uses the last received contact state rather than projecting it forward. | The contact is stationary and continuously fresh; the evaluator grades only ordinary resolution, arrival, and zero collisions. | Extrapolation on and off are effectively indistinguishable in this fixture. | Use a moving contact with a deliberately aging report, then grade a predicted-position, CPA, or trajectory difference attributable to extrapolation. | Open |
| collision_h01 | `pwt_outer_too_small_fail` | The five-meter relevance envelope activates too late to prevent the on-lane collision while the behavior otherwise processes the contact normally. | The evaluator requires only `COLLISION_TOTAL>0`; lifecycle, contact receipt, resolution, and arrival are report-only. | Any unrelated collision-producing failure satisfies the expected-negative verdict. | Require contact receipt and the intended lifecycle/relevance evidence alongside the collision, and compare against the stock-envelope control. | Open |
| collision_h01 | All nine `bad_*_fail` cases | Each specific malformed behavior parameter is what causes configuration rejection. | Every evaluator passes on generic `IVPHELM_STATE` malconfiguration; the recorded `BHV_ERROR` flag is not required and representative rows have it false. | An unrelated helm malconfiguration can make any case pass, and the verdict does not identify the rejected parameter. | Capture and compare a parameter-specific configuration error or warning while retaining the generic malconfiguration requirement. | Open |
| convoy_h01 | `cruise_speed_cap_warn_pass`, `safety_range_autoadjust_warn_pass`, `runtime_cruise_cap_warn_pass` | Each configuration or runtime update emits the warning specific to its invalid or adjusted values. | The evaluator requires only that some `BHV_WARNING` occurred; every committed successful row, including `static_convoy_pass`, records `warning=true`. | A routine baseline warning satisfies all three warning verdicts, so the intended warning is not distinguished. | Capture and compare the warning text or add a case-specific warning flag keyed to the expected parameter and reason. | Open |
| convoy_h01 | `cruise_speed_pass`, `runtime_cruise_speed_pass` | Configured and runtime `cruise_speed=1.2` select a corresponding nonzero chaser speed. | The evaluators require only `ABE_NAV_SPEED<=1.25`, with no positive lower bound; queue and target-motion checks are separate. | A stopped or arbitrarily slow chaser can satisfy the speed assertion. | Add a lower speed bound around the demonstrated 0.96 m/s value and, for the runtime case, observe a pre/post-update change. | Open |
| convoy_h01 | `slow_follower_pass` | Conservative speed multipliers make Abe measurably slower than Ben. | The evaluator grades queue length, trail length, `MXRNG=100`, `AVG2>=1`, and no behavior error; Abe and Ben speeds are report-only. | The named slow-follower behavior is not part of the verdict. | Require Abe's speed in a stable slow band and below Ben's speed by a defined margin. | Open |
| convoy_h01 | `runtime_bad_update_recover_pass` | The invalid update is rejected and the later valid update restores normal operation. | Only the final stock queue/trail/speed state and absence of a behavior error at 45 seconds are graded. | Ignoring both updates, accepting the invalid update, or never entering a failure state can all pass. | Observe a case-specific warning/rejection after the invalid update and a distinct post-recovery state after the valid update. | Open |
| convoy_h01 | `range_aliases_pass` | The three legacy `range_*` aliases are parsed and applied. | The aliases are assigned exactly the stock values, and the evaluator is the stock final-state evaluator. | Ignoring every alias produces the same configuration and verdict. | Give the aliases discriminating nondefault values and grade the resulting estop/tailgating/lagging regime or speed response. | Open |
| convoy_h01 | `tight_radius_pass`, `wide_radius_pass`, `nm_radius_zero_pass`, `safety_off_bad_ranges_pass`, `no_extrapolate_pass` | Each parameter change materially exercises its named capture, safety, or contact-projection path. | All five use the broad stock final-state evaluator; the fixtures do not grade capture events, safety state, or projected contact state. | Ignoring the parameter can plausibly preserve the same passing queue and speed envelope. | Add parameter-specific observations or discriminating geometries: capture timing for radii, a safety-on control for bad ranges, and an aging moving contact for extrapolation. | Open |
| convoy_h01 | `missing_contact_fail`, `missing_contact_param_fail`, and all 12 `bad_*_fail` cases | Each specific malformed or missing parameter is the cause of rejection. | Every evaluator passes on either generic helm malconfiguration or any behavior error. | An unrelated configuration failure can satisfy any case, and the verdict does not identify the rejected field. | Compare parameter-specific warning/error text or another error code while retaining the generic rejection state. | Open |
| cutrange_h01 | `startup_no_warning_pass` | Default CutRange startup produces no behavior warning. | `BHV_WARNING_SEEN` is forcibly reset to false at 42 seconds and graded at 45 seconds. | Any warning produced during actual startup is erased before the verdict. | Preserve a separate startup-warning latch, or rename the case to settled-no-warning and add a true startup interval assertion. | Open |
| cutrange_h01 | `short_time_on_leg_pass`, `long_time_on_leg_pass`, `low_patience_pass`, `max_patience_clamp_pass`, `max_patience_100_pass`, `pwt_outer_active_pass`, `runtime_patience_update_pass`, `no_extrapolate_pass` | Each nondefault parameter or update changes the named projection, weighting, relevance, or contact-state path. | The cases all use the broad stock closure evaluator, with no parameter-specific output or before/after comparison. | Ignoring the parameter or runtime update can plausibly satisfy the same pursuit and closest-range verdict. | Add discriminating geometry and grade a trajectory, objective, activation timing, or before/after range response specific to each parameter. | Open |
| cutrange_h01 | `high_patience_pass` | `patience=85,max_patience=95` still produces meaningful range closure. | The evaluator requires only pursuit plus no warning/error at 45 seconds; it has no range or speed condition. | A pursuing behavior that makes no closure passes. | Add the stock closest-range and target-motion conditions, then distinguish the high-patience trajectory from a lower-patience control. | Open |
| cutrange_h01 | `giveup_dist_alias_pass` | The legacy `giveup_dist` alias is parsed and applied as `giveup_range`. | The alias is set to the stock value 160 and the stock closure evaluator is used. | Ignoring the alias produces the same configuration and verdict. | Use a nondefault alias value in a boundary or runtime-giveup geometry and grade the resulting giveup transition. | Open |
| cutrange_h01 | `runtime_bad_update_recover_warn_pass` | The negative outer-distance update produces its specific rejection warning before valid updates restore active closure. | The evaluator requires any warning plus final closure; it does not clear or identify the known first-active warm-up warning. | A routine warm-up warning can satisfy the rejection evidence, although the final closure does show that valid recovery values took effect. | Reset the warning latch immediately before the invalid update or compare the expected warning text, then retain the final recovery assertions. | Open |
| cutrange_h01 | `missing_contact_fail`, `missing_contact_param_fail`, and all eight `bad_*_fail` cases | Each missing or malformed configuration is rejected and causes the expected inactive mission outcome. | Evaluation occurs at 15 seconds, before the stem posts `CUTRANGE=true` at 30 seconds, and requires only the naturally inactive state, wide separation, stopped Abe, and moving Ben; warning/error mail is report-only. The eight `bad_*` replacement behaviors also omit pursuit/giveup flags. | Every case passes without demonstrating that the behavior activated or rejected the parameter. | Enable CutRange before evaluation, retain explicit pursuit/giveup flags, and require a parameter-specific warning/error or helm-malconfiguration indication. | Open |
| cutrange_h01 | `bad_max_patience_fail` | An out-of-range `max_patience` value is rejected. | The fixture sets `max_patience=100`; current `BHV_CutRange` accepts the inclusive range 0 through 100, and `max_patience_100_pass` intentionally exercises that valid endpoint. | The fixture is not malformed, independent of the already-vacuous evaluator. | Change the value to 101 or another genuinely invalid input, then grade explicit rejection after behavior activation. | Open |
| fixedturn_h01 | `starboard_360_pass`, `port_360_pass` | Each configured 355-degree turn completes in the requested direction and produces valid radial statistics. | The evaluator requires only completion, turn time above 15 seconds, distance above 25 meters, and no error; the stock 90-degree result already records 21.84 seconds and 29 meters. Heading, direction, and `FT_REPORT` are report-only. | An ordinary 90-degree turn satisfies the long-turn verdict, so neither near-full rotation nor direction is proved. | Require accumulated heading change or a stable final-heading/trajectory signature and parse the radius fields from `FT_REPORT`, with direction-specific rudder or path evidence. | Open |
| fixedturn_h01 | `speed_auto_pass`, `fixed_speed_pass`, `runtime_static_update_pass` | `speed=auto`, fixed speed 1.3, and runtime speed 1.6 select their intended turn speeds. | Each evaluator grades direction/completion and broad time/distance only; `NAV_SPEED` and `FT_REPORT` speed are report-only. | Ignoring the speed setting or update can pass. | Parse `FT_REPORT`'s `spd=` field or grade a stable speed band during the turn; retain the direction assertion for the runtime case. | Open |
| fixedturn_h01 | `turn_delay_pass` | `turn_delay=4` holds the initial heading for four seconds before turning. | The evaluator grades only eventual completion, positive time/distance, and no error. | A behavior that starts turning immediately passes. | Observe unchanged heading during the delay and a later heading change, or grade activation-to-turn timing around four seconds. | Open |
| fixedturn_h01 | `turn_spec_sequence_pass` | The scheduled starboard 45-degree turn and subsequent port 45-degree turn both execute in order. | The evaluator requires both flags, final completion, positive final-turn time/distance, and no error, but no intermediate or final heading. | Flags may post even if one or both turn directions/angles are wrong. | Latch heading after `TURN_ONE` and after `TURN_TWO`, or record cumulative direction-specific heading changes for both entries. | Open |
| fixedturn_h01 | `turn_spec_bad_update_recover_pass` | The malformed keyed update is rejected before the valid keyed replacement executes. | The evaluator proves that original and replacement flags post and the valid starboard turn completes, but it does not require a warning or other evidence for the malformed update. | Ignoring the malformed update produces the same verdict. | Require the expected update-rejection warning or a keyed-schedule state snapshot before applying the valid replacement. | Open |
| fixedturn_h01 | `zero_fix_turn_pass` | `fix_turn=0` completes immediately without moving through a turn. | The evaluator requires completion in under two seconds and no error; distance and heading are report-only. | A short unintended motion can pass. | Require `TURNING_DIST` near zero and heading to remain near the activation heading. | Open |
| fixedturn_h01 | All six `bad_*_fail` cases | Each specific malformed field is rejected. | The evaluator requires a stopped vehicle, unchanged heading, no completion by timeout, and no behavior error, but no rejection warning or parameter-specific evidence. | Any unrelated failure that prevents helm motion satisfies every case. | Require a parameter-specific configuration warning/error or helm-malconfiguration state while retaining the no-motion outcome. | Open |
| periodic_speed_h01 | All 12 default positive cases and manual `reset_false_visual_pass` | Each sampled periodic state is reached without a behavior error and at the scheduled evaluation time. | `BHV_ERROR_SEEN`, `MISSION_DONE`, and `MISSION_TIMEOUT` are report-only; evaluators can grade from the later timeout and do not reject behavior errors. | A case may pass after its intended sampling time or with a concurrent behavior error if the remaining state happens to match. | Require `MISSION_DONE=true`, `MISSION_TIMEOUT=false`, and `BHV_ERROR_SEEN=false` in every positive evaluator. | Open |
| periodic_speed_h01 | `deprecated_zaic_aliases_pass`, `zaic_base_period_peak_aliases_pass` | The deprecated ZAIC width/peak/summit aliases are parsed and control the generated speed objective. | The evaluator proves only the separately configured period timing and `period_speed=1.5`; it does not inspect the objective shape or alias-derived parameters. | Ignoring the ZAIC aliases while honoring period timing and speed passes. | Inspect an IvP-function diagnostic or use discriminating width/peak/summit values with a measurable speed-response envelope. | Open |
| periodic_speed_h01 | `explicit_initially_lazy_pass` | Explicit `initially_busy=false` is parsed and selects the lazy initial state. | False is already the default, and the evaluator is equivalent to `lazy_wait_pass`. | Ignoring the explicit parameter produces the same verdict. | Pair it with a nondefault prior/runtime state or otherwise create a fixture where only parsing the explicit false value restores the lazy start. | Open |
| periodic_speed_h01 | All 12 `bad_*_fail` cases | Each specific malformed field is what causes helm malconfiguration. | Every evaluator requires only generic `IVPHELM_STATE=HELM_MALCONFIG`. | An unrelated helm configuration failure satisfies any case, and the rejected field is not identified. | Capture and compare parameter-specific configuration error text or another field-specific rejection signal. | Open |
| zigzag_h01 | `single_leg_complete_pass`, `zero_leg_limit_pass` | The one-leg and zero-leg limits produce their distinct documented completion counts. | Both cases use the same evaluator, requiring only behavior completion, `ZIG_COUNT>=1`, port entry and exit, and no behavior error. | The verdict does not distinguish zero from one or reject extra legs; either fixture can satisfy the other's grade. | Require exact zig/zag counts for each limit and any final-side evidence needed to distinguish their stopping points. | Open |
| zigzag_h01 | `mod_speed_reset_pass` | `mod_speed=0.8` first increases speed and `mod_speed=reset` subsequently restores the captured activation speed. | Only the final `NAV_SPEED` band from 1.0 to 1.9 m/s is graded. | Ignoring both runtime updates can leave the baseline speed in the accepted final band. | Grade a high-speed interval after the modifier, followed by a return to a narrow captured-speed band after reset. | Open |
| zigzag_h01 | `bad_runtime_update_recover_pass` | Invalid `zig_angle=0` is rejected without preventing the later valid `mod_speed=0.4` update. | The final behavior completes with `NAV_SPEED>1.45` and no `BHV_ERROR`; no update warning or unchanged angle is observed. | The verdict proves that a later speed update takes effect but not that the first update was specifically rejected or left the angle unchanged. | Match the rejection warning and grade heading/leg evidence showing the original 25-degree angle remains in force before or after the valid speed update. | Open |
| zigzag_h01 | `fierce_zigging_pass`, `delta_heading_alias_pass` | Fierce mode uses the configured 45- or 40-degree fierce heading delta, and the alias has the same semantics as the canonical parameter. | Both cases grade completion, side/count flags, and final stem odometry below 25 meters; their base `zig_angle` values also differ. | The actual fierce heading delta is not observed, and the alias is not compared under an otherwise identical fixture. | Grade requested/achieved heading extrema or another fierce-specific diagnostic, and make the alias fixture identical to the canonical case except for the parameter spelling. | Open |
| zigzag_h01 | `visual_hints_off_pass`, `visual_hints_custom_pass` | Drawing is disabled or uses the requested custom colors. | Both cases use the ordinary baseline evaluator and do not inspect `VIEW_*` publications. | Invalid, ignored, or unchanged visual output can pass as long as motion completes. | Observe the relevant visualization messages, requiring absence when disabled and the configured colors when enabled. | Open |
| zigzag_h01 | All ten `bad_*_fail` configuration cases | Each named invalid parameter is the cause of behavior rejection. | Every case passes on generic `IVPHELM_STATE` malconfiguration. | An unrelated helm configuration error satisfies any case, and the verdict does not identify the rejected parameter. | Capture and compare a parameter-specific error or warning while retaining the generic malconfiguration state. | Open |
| legrun_h01 | `init_far_turn_pass` | `init_leg_mode=far_turn` starts directly on the far-side leg rather than following the default initialization sequence. | The evaluator requires only `LR_LEG2_DONE=true`, `LR_DONE=true`, and no behavior error; `LR_LEG1_DONE=false` is report-only. | A default run that completes leg 1 and then leg 2 can satisfy the same grade. | Require the far-leg flag before any leg-1 flag, or explicitly require leg 1 to remain false through completion. | Open |
| legrun_h01 | `turn_rad_alias_pass`, `turn_bias_ext_gap_pass`, `individual_turn_params_pass`, `turn_rad_min_clip_pass` | The configured aliases, clipping rule, and asymmetric parameters construct the intended turn geometry, including turn 2. | Every case uses the baseline evaluator, which grades event flags only through turn 1 and leg 2; representative rows have `LR_TURN2_DONE=false` and no geometry is measured. | Turn-2 parameters are not exercised before grading, and many incorrect turn-1 geometries can still reach the same flags. | Continue through turn 2 and grade turn-direction, radius, extension, or turn-point geometry for both generated turns. | Open |
| legrun_h01 | `leg_speed_schedule_pass`, `leg_speed_onturn_pass`, `leg_speed_count_repeat_pass`, `runtime_speed_replace_reset_pass` | Static and runtime schedule syntax produces the specified speed sequence on legs and, where requested, turns. | The evaluators grade only progression flags and no behavior error; `NAV_SPEED` is report-only and update history is not captured. | Ignoring the schedule or any replace/reset/clear update can pass. | Latch the speed at each relevant leg/turn transition and compare the exact expected sequence, including pre- and post-runtime-update phases. | Open |
| legrun_h01 | `runtime_length_angle_update_pass`, `runtime_turn_radius_update_pass`, `runtime_shift_point_pass`, `runtime_turn_bias_gap_update_pass` | Each runtime update changes the requested leg or turn geometry. | All four cases grade only ordinary progression, midpoint, completion, and no-error flags. | Ignoring every geometry update can produce the same verdict. | Grade updated endpoint/turn diagnostics or trajectory bands that distinguish each new geometry from the stem configuration. | Open |
| legrun_h01 | `mid_pct_late_pass` | Seventy-percent midpoint flags fire later in each leg and turn than the default thirty-percent flags. | The evaluator requires only that both flags have fired by final completion. | A behavior that ignores the percentages and fires at 30 percent passes. | Observe segment progress when each flag arrives and require it within a narrow band around 70 percent. | Open |
| legrun_h01 | `patience_clip_pass` | `patience=150` is clipped to the supported maximum 99 and used by the objective function. | `BHV_SETTINGS` reports `patience=99`, but the evaluator grades only ordinary progression flags and no behavior error. | An unclipped, ignored, or incorrectly reported patience value does not fail the case. | Parse and require `patience=99` in `BHV_SETTINGS`; add objective-function evidence if use of the clipped value is also intended. | Open |
| legrun_h01 | All nine `bad_*_fail` cases | Each malformed field prevents configuration for the named reason. | Every case passes merely when the mission times out, `LR_TURN2_DONE=false`, and no `BHV_ERROR` mail was seen. | Any unrelated stall before turn 2 passes, and the evaluator neither observes helm malconfiguration nor identifies the bad field. | Require a parameter-specific configuration warning/error or helm rejection state; use timeout only as secondary no-progress evidence. | Open |
| memoryturnlimit_h01 | `tight_window_pass`, `relaxed_window_pass` | The change in `turn_range` is responsible for the tighter or looser heading response. | The tight case changes both range from 18 to 6 and weight from 150 to 200; the relaxed case changes range to 120 and weight to 100. | The observed response cannot be attributed specifically to `turn_range`, because priority weight changes in the same direction. | Hold weight at 150 while varying only range; retain `weak_priority_pass` as the separate weight fixture. | Open |
| memoryturnlimit_h01 | `zero_memory_pass` | `memory_time=0` uses the documented current-heading boundary semantics rather than a short positive memory. | Its accepted average, heading, and mismatch bands substantially overlap `short_memory_pass` with `memory_time=2`. | Treating zero as two seconds can satisfy both evaluators. | Use a trajectory or evaluation instant where zero and two seconds produce nonoverlapping averages, or directly compare the reported memory window/state. | Open |
| memoryturnlimit_h01 | `bad_memory_time_fail`, `bad_turn_range_high_fail`, `bad_turn_range_text_fail` | Each named malformed parameter causes its own launch-time rejection. | Every case grades only generic helm `MALCONFIG` plus near-zero speed. | An unrelated malformed behavior or parameter can satisfy all three. | Capture parameter-specific helm/behavior error text while retaining the malconfiguration and stopped-vehicle checks. | Open |
| memoryturnlimit_h01 | `missing_memory_time_fail`, `missing_turn_range_fail` | The warning identifies the specific required parameter that was omitted. | Any `BHV_WARNING` sets the shared flag; warning text is not compared. | An unrelated warning satisfies either case, so the two omissions are not distinguished. | Compare the warning payload with the expected missing-`memory_time` or missing-`turn_range` message. | Open |
| timer_h01 | `custom_status_vars_pass`, `default_suffix_pass`, `custom_no_suffix_pass`, `default_no_suffix_pass` | The selected names and suffix replace the other default or configured status outputs. | Each evaluator requires only that the intended two counters exceed two seconds. | Duplicate publications under unintended names or suffixes do not fail. | Add absence checks for the superseded stock/custom names appropriate to each case. | Open |
| timer_h01 | `active_at_start_pass` | Immediate activation produces essentially no idle accumulation before the running interval. | Only running time greater than five seconds is graded; idle time is report-only. | A timer that incorrectly accumulates substantial idle time can pass. | Require idle time near zero while retaining the running-time lower bound. | Open |
| timer_h01 | `repeated_status_param_pass` | Later repeated parameter values replace the earlier `UNUSED_*` names and `unused` suffix. | The final stock suffixed counters must exceed two seconds, but the earlier output names are not observed. | Implementations that publish both the earlier and later settings pass. | Fail on any `UNUSED_IDLE_unused`, `UNUSED_RUN_unused`, or other superseded-name publication. | Open |
| timer_h01 | `post_mapping_pass` | `post_mapping` moves both status posts to the mapped variables rather than duplicating them. | The mapped counters must exceed two seconds; original suffixed counters are report-only. | Correct mapped publications can coexist with incorrect original publications. | Fail on any original `TIMER_IDLE_SECONDS_base` or `TIMER_RUNNING_SECONDS_base` post. | Open |
| timer_h01 | `bad_idle_var_fail`, `bad_running_var_fail`, `bad_suffix_fail`, `unknown_param_fail` | The named invalid field is rejected and prevents all timer status output. | The evaluator only forbids the two stock suffixed counters and requires no `BHV_ERROR`; it observes no configuration warning or helm state. | A different output name can appear, and no evidence identifies the rejected parameter or even proves configuration rejection caused the absence. | Capture a parameter-specific warning/rejection and fail on any timer status output, not only the two stem names. | Open |
| testfailure_h01 | `burn_default_time_gap_pass`, `burn_malformed_time_default_gap_pass` | Omitted or malformed burn time preserves the constructor's three-second default. | Both evaluators require only a latched helm iteration gap greater than 1.5 seconds, completion, and process survival. | Any burn duration above 1.5 seconds, including the explicit two-second behavior, satisfies the claimed default-time cases. | Grade the latched gap in a band around three seconds and add an upper bound that distinguishes it from the two-second fixture. | Open |
| processwatch_h01 | `processwatch_all_present_pass`, `processwatch_multi_present_pass` | The summary payloads contain the expected `All Present` text and complete watched-process counts. | The evaluators require only `PROC_WATCH_ALL_OK=true` and nonempty summary strings. | Malformed, partial, or incorrect summary content passes. | Compare the short summary exactly and parse the full summary to require each watched process and its expected present/AWOL counts. | Open |
| processwatch_h01 | `processwatch_post_mapping_pass`, `processwatch_full_summary_mapping_pass`, `processwatch_event_mapping_present_pass` | Each mapped output carries the correct source payload and the original post is replaced according to mapping semantics. | The mapped variables and selected ordinary summaries need only be nonempty. | Any nonempty mapped value passes; payload identity, event content, and original-output suppression are untested. | Compare the mapped payload exactly with the expected summary/event and fail if a source variable that should be replaced is published. | Open |
| processwatch_h01 | `processwatch_missing_awol_fail` | The AWOL summary specifically names `pGhost` with one configured and one missing instance. | Only `PROC_WATCH_ALL_OK=false` and `GHOST_PRESENT=false` are graded; both summary fields are report-only. | The case does not prove the summaries identify the missing process or contain correct counts. | Require `PROC_WATCH_SUMMARY="AWOL: pGhost"` and parse `PROC_WATCH_FULL_SUMMARY` for `pGhost(1/1)`. | Open |
| pshare_h01 | `pshare_rename_route_pass`, `pshare_shorthand_route_pass`, `pshare_wildcard_prefix_rename_pass`, `pshare_wildcard_caret_rename_pass` | Rename syntax publishes only the computed destination variable rather than also leaking the source name. | Each evaluator requires only the expected destination value. | Correct destination delivery can coexist with an incorrect source-name delivery. | Add absence checks for each original source variable at the receiver. | Open |
| pshare_h01 | `pshare_wildcard_source_app_pass` | The `:uTimerScript` qualifier accepts matching mail and rejects the same variable from another source app. | Only a matching uTimerScript post is sent and its arrival is graded. | Ignoring the source-app qualifier produces the same result. | Post the same wildcard-matching variable from a second app with a distinguishable value and require that value not to route. | Open |
| pshare_h02 | `pshare_topology_competing_update_pass` | Alpha's earlier value arrives before bravo's later update overwrites it. | Only the final `COMPETE_RX=bravo` value is graded at 45 seconds. | A broken alpha route with a working bravo route passes. | Latch `COMPETE_RX=alpha` before bravo posts, then require the final bravo value. | Open |
| plogger_h01 | `plogger_double_log_pass` | The numeric value `42.5` is preserved in the generated `.alog`. | The live value must be between 42 and 43, while the artifact check requires only an `.alog` line containing `PLOGGER_NUM`. | An incorrect logged value passes as long as the variable name appears. | Match the `.alog` entry against `PLOGGER_NUM` and the expected numeric value `42.5`. | Open |
| pantler_h01 | `pantler_system_path_launch_pass` | `PATH=SYSTEM` controls how pAntler locates the aliased executable. | `uTimerSystemPath` must post `ANTLER_PATH_FLAG=true`. | The same executable is already available to ordinary launch lookup, so ignoring `PATH=SYSTEM` can produce the same result. | Put a case-specific executable or shim in a controlled system-search directory that cannot be found through the default lookup path, then require its unique post. | Open |
| pantler_h01 | `pantler_extra_params_launch_pass` | `ExtraProcessParams=timer_args` forwards `--verbose=false` to the child process. | `uTimerExtra` must post `ANTLER_EXTRA_FLAG=true`. | The post proves that the child launched, but not that it received or honored the extra argument. | Forward an argument with a distinct observable effect and grade that effect, or launch a probe executable that reports its received argv. | Open |
| pmissioneval_h01 | `eval_flags_and_mail_pass` | The result, pass, and mail flags contain `result:alpha`, `pass:alpha`, and `true`, respectively. | The artifact check requires only that `EVAL_RESULT_FLAG`, `EVAL_PASS_FLAG`, and `EVAL_MAIL_SEEN` appear in the `.alog`. | Incorrect payloads pass as long as all three variables are published. | Match each `.alog` entry against its exact expected value. | Open |
| pmissionhash_h01 | `hash_custom_vars_pass` | Custom publication names replace the default `MISSION_HASH` and `MHASH` variables. | The report row validates values received through `CUSTOM_MISSION_HASH` and `CUSTOM_MHASH_SHORT`; default variables are not checked for absence. | Correct custom posts can coexist with incorrect default-name posts. | Fail if either default variable appears while retaining the custom-value format checks. | Open |
| umayfinish_h01 | `mayfinish_custom_value_timeout_fail` | A received `CUSTOM_DONE=almost` post is ignored because it does not equal the configured finish value `complete`. | The directly launched process exits with timeout code 1; the harness does not inspect `CUSTOM_DONE` mail. | Failure to publish or receive `CUSTOM_DONE` is indistinguishable from correctly rejecting the wrong value. | Require an `.alog` entry for `CUSTOM_DONE=almost` before accepting the timeout result. | Open |
| loiter_h01 | `radial_counterclockwise_pass`, `clockwise_best_pass` | `clockwise=false` reverses traversal and `clockwise=best` selects the approach-appropriate direction. | Both cases grade only the final index, stable/acquire status, distance to polygon, and absence of `BHV_ERROR`. | The chosen direction is never observed; ignoring the parameter can pass. | Grade signed index progression, heading/bearing evolution, or another direction-specific output, and give `best` an approach where the expected choice is known. | Open |
| loiter_h01 | `polygon_box_pass`, `triangle_polygon_pass` | BHV_Loiter follows the supplied rectangle or triangle rather than the stock radial polygon. | The final distance reported by the behavior must be at most 10 with stable status. | Ignoring the patched geometry and following the stock polygon can plausibly satisfy the same broad final conditions. | Check vehicle proximity to multiple shape-specific edges/vertices or compare the published polygon/trajectory against each exact fixture. | Open |
| loiter_h01 | `acquire_from_far_pass` | The behavior enters external-acquisition mode before transitioning to stable loiter. | Only the final `LOITER_ACQUIRE=0`, stable mode, and distance band are graded at 25 seconds. | A behavior that skips or misreports the acquisition phase can pass. | Latch `LOITER_ACQUIRE=1` while outside before requiring the final transition to zero and stable mode. | Open |
| loiter_h01 | `early_acquire_mode_pass`, `slow_speed_acquire_pass` | The behavior reports `LOITER_MODE=acquiring_external` while the acquisition flag is set. | Both evaluators require `LOITER_ACQUIRE=1` and distance greater than 10 but do not grade `LOITER_MODE`. | An incorrect mode string passes. | Add `LOITER_MODE=acquiring_external` to both pass-condition sets. | Open |
| loiter_h01 | `center_activate_pass` | `center_activate=true` relocates the polygon center to the vehicle position at activation. | Only final stable status and distance to the behavior-reported polygon are graded. | The evaluator never compares the resulting center with the activation position. | Inspect the published polygon center or use a trajectory gate that the original center fails and the activation-centered polygon passes. | Open |
| loiter_h01 | `capture_radius_large_pass` | The larger capture radius, rather than the simultaneously enlarged slip radius, changes point-capture behavior. | Final stable-loiter conditions are graded after changing both radii. | The two parameters are confounded and neither capture history nor capture counters are observed. | Vary one radius at a time and grade `capture_hits`, index-transition timing, or another threshold-specific event. | Open |
| loiter_h01 | `slip_radius_pass` | A 0.5-meter capture radius forces the wider 12-meter slip/nonmonotonic capture path. | Only final stable-loiter conditions are graded. | The case does not require a slip/nonmonotonic capture event, and the named `slip_radius` is unchanged from the stem. | Grade a positive `nonmono_hits`/slip counter or an index transition proven to occur outside 0.5 meters but inside 12 meters; consider renaming the fixture. | Open |
| loiter_h01 | `speed_alt_update_pass`, `use_alt_speed_static_pass`, `ipf_zaic_spd_pass` | Runtime/static alternate-speed selection and the ZAIC speed IPF change the speed objective used by the behavior. | Each case grades only final stable-loiter status at 25 seconds. | No desired or realized speed, update acceptance, or IPF-specific output is observed. | Grade a speed band before and after the update, a static alternate-speed band, and an IPF construction/status signal or discriminating trajectory timing. | Open |
| loiter_h01 | `center_assign_xy_pass`, `center_assign_pair_pass`, `xcenter_ycenter_update_pass` | Each accepted center-update syntax moves the polygon to its requested coordinates. | Each case grades only final stable status and distance to whichever polygon the behavior reports. | Ignoring the updates can still pass around the stock center. | Compare the published polygon center with the requested coordinates or require passage through location-specific gates. | Open |
| loiter_h01 | `mod_poly_rad_expand_pass`, `mod_poly_rad_shrink_pass` | `mod_poly_rad` changes the radius by exactly +6 or -5. | Both cases grade only final stable status and distance to polygon. | Ignoring the update or applying the wrong magnitude can pass. | Parse the published polygon radius or grade vehicle crossings/timing that distinguish radii 24 and 13 from the stock radius 18. | Open |
| loiter_h01 | `post_suffix_outputs_pass` | `post_suffix=custom` replaces unsuffixed status publications with suffixed variables. | The evaluator requires the suffixed values but never checks the unsuffixed variables for absence. | Correct suffixed posts can coexist with incorrect stock posts. | Fail on any post to the superseded unsuffixed loiter status variables. | Open |
| loiter_h01 | `bad_polygon_fail`, `empty_polygon_fail`, `bad_clockwise_fail`, `bad_use_alt_speed_fail`, `bad_patience_fail`, `bad_capture_radius_fail` | Each malformed configuration causes parameter-specific helm `MALCONFIG`. | `mailflag=@IVPHELM_STATE#HELM_MALCONFIG=true` sets the latch on any mail to `IVPHELM_STATE`; its payload is not compared. | A normal helm-state publication can pass every negative case, and the cases are not distinguished. | Compare `IVPHELM_STATE` with `MALCONFIG` and capture a parameter-specific `BHV_ERROR` or warning for each fixture. | Open |
| loiter_h01 | `bad_update_fail` | The self-intersecting runtime polygon is rejected and the original polygon remains active. | Any `BHV_WARNING` plus final stock-like stable-loiter conditions passes. | Warning content is not checked, and final conditions do not prove the malformed polygon was rejected rather than accepted or ignored for another reason. | Match the warning payload and compare the post-update published polygon with the original valid polygon. | Open |
| loiter_h01 | `center_bad_update_recover_pass` | The malformed center update is rejected and the later valid update moves the center to `(18,-121)`. | Only final stable-loiter conditions are graded; no warning or center value is observed. | Ignoring both updates passes. | Require a bad-update warning, then compare the published center with `(18,-121)` after recovery. | Open |
| loiter_h01 | `spiral_factor_pass`, `patience_low_pass`, `patience_high_pass` | The nondefault spiral factor and patience values change acquisition or point-switching behavior. | All three cases use the same broad final stable-loiter evaluator. | Ignoring these parameters can pass, and low versus high patience is not distinguished. | Grade acquisition curvature/timing for `spiral_factor` and index-switch timing or course/speed objective ratios for the two patience values. | Open |
| obstacle_behavior_h01 | `rng_flag_pass`, `rng_flag_no_threshold_pass`, `cpa_flag_pass` | The configured optional flags publish the exact value `true` under their threshold/CPA conditions. | Each mailflag sets a helper boolean true on any post to `AVOID_RANGE_SEEN` or `AVOID_CPA_SEEN`; payloads are not compared. | A false or malformed payload passes, and the no-threshold form is not distinguished by its value. | Compare each optional-flag publication with the exact configured payload and retain the threshold/CPA timing conditions. | Open |
| obstacle_behavior_h01 | `no_refinery_pass` | `use_refinery=false` selects the non-refinery IvP objective path. | The case requires the same request, alert, lifecycle-end, arrival, and collision outcome as the default case. | Ignoring `use_refinery=false` and continuing through the refinery can pass. | Expose or instrument objective-construction mode, or use a discriminating fixture/mutation showing the refinery and non-refinery paths produce distinguishable evidence. | Open |
| obstacle_behavior_h01 | `bad_polygon_fail`, `bad_pwt_inner_dist_fail`, `bad_pwt_outer_dist_fail`, `bad_completed_dist_fail`, `bad_min_util_cpa_dist_fail`, `bad_max_util_cpa_dist_fail`, `bad_allowable_ttc_fail`, `bad_rng_flag_fail`, `bad_cpa_flag_fail`, `bad_use_refinery_fail`, `bad_pwt_grade_fail` | Each malformed parameter produces its own helm `MALCONFIG` rejection. | `mailflag=@IVPHELM_STATE#HELM_MALCONFIG=true` latches on any `IVPHELM_STATE` mail; `helm_state` is reported but not compared. | A normal state publication can satisfy every case, and no evidence distinguishes the rejected field. | Require `IVPHELM_STATE=MALCONFIG` and match a parameter-specific `BHV_ERROR` or warning for each fixture. | Open |
| opregion_h01 | `save_recover_pass`, `save_dist_buffer_fail`, `dynamic_region_update_pass` | A save-region breach also activates the configured `savex_flag=SAVE_REGION_RECOVERING=true`. | The evaluators require `SAVE_REGION_BREACHED=true`; `SAVE_REGION_RECOVERING` is report-only. | Recovery-flag publication can regress without failing any save-breach case. | Add `SAVE_REGION_RECOVERING=true` to the relevant pass conditions, or split breach and recovery timing into separate latched evidence. | Open |
| opregion_h01 | `trigger_exit_debounce_pass`, `trigger_entry_delay_pass` | The vehicle actually crosses the relevant halt boundary during the timing window, and the configured delay suppresses the breach. | At 44 seconds the evaluator requires only absence of halt/time breach and behavior error. | A stalled or altered route that never makes the excursion passes. | Latch boundary-exit/entry geometry from `NAV_X/Y` or a save-breach flag before grading the absence of a halt breach; retain the strict companion as a mutation control. | Open |
| opregion_h01 | `reset_before_exit_pass` | `OPREGION_UPDATES=reset=true` clears entered-state and is necessary to suppress a pending halt breach. | The evaluator only requires no halt/time breach or behavior error at 44 seconds; `trigger_exit_time=20` already exceeds the short excursion duration. | Ignoring the reset can pass because the vehicle returns before the debounce timer expires. | Shorten `trigger_exit_time` so the no-reset fixture fails, prove the update was received, and retain a paired mutation with the reset post removed. | Open |
| opregion_h01 | `max_depth_breach_fail`, `min_altitude_breach_fail` | The error is specifically a max-depth or minimum-altitude violation caused by the posted sensor value. | Any `BHV_ERROR` mail satisfies the evaluator while other breach flags remain false. | An unrelated behavior error passes and the error payload is not tied to the configured threshold. | Match the exact OpRegion depth/altitude error text and report the triggering `NAV_DEPTH`/`NAV_ALTITUDE` value. | Open |
| opregion_h01 | `bad_save_dist_fail`, `bad_halt_dist_fail`, `bad_max_depth_fail`, `bad_min_altitude_fail`, `bad_recover_spd_fail`, `bad_trigger_on_poly_entry_fail`, `bad_trigger_entry_time_fail`, `bad_trigger_exit_time_fail` | Each invalid value causes parameter-specific helm `MALCONFIG`. | `mailflag=@IVPHELM_STATE#HELM_MALCONFIG=true` latches on any `IVPHELM_STATE` publication, without comparing its payload. | Normal helm state can satisfy every negative case and the rejected parameter is unidentified. | Require `IVPHELM_STATE=MALCONFIG` and compare a parameter-specific `BHV_ERROR` or warning payload. | Open |
| p01_obstacle | `baseline_field_pass`, `dense_field_pass` | The baseline case stages the six-obstacle file and the dense case stages the distinct eight-obstacle file. | Both cases use identical mission thresholds and shell wall/warning bands; no field identity or obstacle count is reported. | Staging the baseline file for both cases can pass, especially because their observed aggregate counts are currently identical. | Report a field hash or obstacle count and require six versus eight, then retain metric bands appropriate to each fixture. | Open |
| p03_colregs | `mixed_speed_circle_pass` | The five vehicles use the configured asymmetric speeds `1.55,1.67,1.79,1.91,2.03`. | The evaluator grades completion, collision, closest-range, batch, timeout, wall-time, and warning metrics only. | Launching equal speeds can pass. | Report each vehicle's configured or observed speed and require the five-value profile before accepting performance metrics. | Open |
| p03_colregs | `noncoop_circle_pass` | `eve` is the selected non-cooperative vehicle and actually runs with `AVOID=false`. | `DUMB_VNAME` is report-only, and no helm mode or avoidance state is graded. | A fully cooperative ring or a different disabled vehicle can pass. | Require `DUMB_VNAME=eve` and observable evidence from eve that avoidance remained disabled throughout the window. | Open |
| shadow_h01 | `startup_no_warning_pass`, `static_shadow_pass` | The startup case uniquely demonstrates that delaying `contact=ben` and Shadow activation avoids a premature missing-contact warning. | Both cases use the same stem events (`contact=ben` at 30 seconds and `SHADOW=true` at 32 seconds); the startup evaluator merely lowers Abe's minimum speed from `1.0` to `0.8`. | There is no distinct startup fixture or pre-activation observation, so the two cases can pass for the same reason. | Activate Shadow before contact data in a paired control, or explicitly observe the pre-activation interval and then retain the warning-free delayed case. | Open |
| shadow_h01 | `pwt_outer_active_pass`, `pwt_outer_edge_pass` | `pwt_outer_dist=120` and `100` keep Shadow relevant because the contact is inside each configured boundary. | Final positive relevance and broad motion bounds. | The stem uses unrestricted `pwt_outer_dist=0`; an implementation that ignores both case patches can satisfy both grades, and the actual range is not compared with the boundary. | Grade `SHADOW_CONTACT_RANGE` or equivalent against the threshold and add just-inside/just-outside paired geometry that fails when the parameter is ignored. | Open |
| shadow_h01 | `runtime_bad_update_recover_warn_pass`, `runtime_missing_contact_recover_warn_pass` | Each invalid runtime update emits its expected warning before a later valid update restores active shadowing. | Any `BHV_WARNING` sets the warning latch; final relevance and speed evidence proves only the recovered state. | The warning payload is not tied to the invalid value or missing contact, and no observation proves the intermediate state. | Compare the warning text and latch the inactive/missing-contact interval before checking final recovery. | Open |
| shadow_h01 | `heading_widths_pass`, `speed_width_aliases_pass`, `no_extrapolate_pass` | Nondefault objective widths, accepted width aliases, and disabled extrapolation each affect their intended Shadow code path. | Each uses the unchanged default positive-relevance, heading, speed, and no-warning evaluator. | Ignoring the changed parameters can produce the same result. | Add objective/debug telemetry or discriminating contact maneuvers where each setting changes a measurable heading/speed response; confirm with parameter-ignored mutations. | Open |
| shadow_h01 | `missing_contact_warn_pass`, `missing_contact_fail` | The unavailable `ghost` contact produces the policy-specific Shadow warning or error. | Any `BHV_WARNING` or `BHV_ERROR` publication satisfies the respective latch. | An unrelated warning or error passes, so neither payload is tied to the missing-contact policy. | Compare the exact missing-contact warning/error payload and retain the absence of `BHV_ERROR` in the warning-only case. | Open |
| shadow_h01 | `missing_contact_param_fail`, `bad_pwt_outer_dist_fail`, `bad_heading_peakwidth_fail`, `bad_heading_basewidth_fail`, `bad_speed_peakwidth_fail`, `bad_speed_basewidth_fail`, `bad_decay_fail`, `bad_decay_order_fail`, `bad_extrapolate_fail`, `bad_on_no_contact_ok_fail`, `bad_time_on_leg_fail`, `bad_cnflag_fail`, `bad_post_per_contact_info_fail` | Each malformed value causes its parameter-specific helm `MALCONFIG` rejection. | `mailflag=@ABE_IVPHELM_STATE#HELM_MALCONFIG=true` sets the latch on any helm-state publication; the raw state is report-only. | A normal helm-state publication can pass every negative case, and no evidence identifies the rejected parameter. | Require `ABE_IVPHELM_STATE=MALCONFIG` and compare a parameter-specific `BHV_ERROR` or warning payload for every fixture. | Open |
| stationkeep_h01 | `center_activate_swing_pass` | `swing_time=8` changes the activation-centered holding behavior relative to plain `center_activate=true`. | The case uses the same final `DIST_TO_STATION<=8` and no-warning/no-error grade as `center_activate_hold_pass`. | Ignoring `swing_time` can pass, and no trajectory or mode evidence distinguishes the swing case. | Compare early position, heading, or StationKeep state across the plain and swing fixtures, and confirm the swing-time setting from graded behavior output. | Open |
| stationkeep_h01 | `wide_radius_pass`, `tight_radius_pass`, `inner_gt_outer_pass`, `radius_update_expand_pass`, `radius_update_shrink_pass` | Each static or runtime radius configuration changes the inner/outer ring behavior. | Final distance upper bounds range from `9` to `18`; `BHV_SETTINGS` contains the radii but is report-only. | The default controller already reaches within these upper bounds, so ignoring the radius changes can pass. | Grade the exact radii in `BHV_SETTINGS` and add timing/mode or motion evidence that discriminates the ring behavior, including an explicit policy assertion for inner-greater-than-outer. | Open |
| stationkeep_h01 | `outer_speed_slow_pass` | Static `outer_speed=0.35` slows correction inside the outer ring. | At 50 seconds only `DIST_TO_STATION<=20` and no error are required. | The faster default controller also satisfies the upper bound. | Add a lower distance bound or timed position band that distinguishes `0.35` from `1.2`, and grade the setting from `BHV_SETTINGS`. | Open |
| stationkeep_h01 | `transit_speed_fast_pass`, `extra_speed_alias_pass` | `transit_speed=3.0` and alias `extra_speed=3.0` produce a faster approach than the stock speed. | The fast case uses the baseline 35-second `DIST_TO_STATION<=8` grade; the alias case requires only `DIST_TO_STATION<=25` at 20 seconds. | The stock-speed fixture can satisfy both grades, so ignored parameters can pass. | Add earlier, tighter distance bands and grade the accepted transit-speed value; retain a stock-speed control at the same evaluation times. | Open |
| stationkeep_h01 | `transit_speed_slow_in_progress_pass`, `speed_update_slow_progress_pass` | Static and runtime slow transit speeds produce bounded forward progress rather than merely remaining far away. | At 15 seconds each requires only `DIST_TO_STATION>20` and no error. | A stationary or inactive vehicle at the roughly 57-meter initial range passes. | Add an upper distance bound below the initial range, require `PSKEEP_MODE=SEEKING_STATION`, and grade the configured speed or observed motion. | Open |
| stationkeep_h01 | `hibernation_off_pass` | The explicit token `hibernation_radius=off` selects disabled hibernation rather than only parsing successfully. | The ordinary final `DIST_TO_STATION<=8` and no-warning/no-error grade. | The default already has hibernation disabled, so ignoring the token produces the same result. | Grade `hibernation_radius=-1` in settings and pair it with an enabled fixture at a time/range where `PSKEEP_MODE` differs. | Open |
| stationkeep_h01 | `point_update_retarget_pass` | The live update changes the active station from `(12,-121)` to `(24,-121)`. | Final behavior-owned `DIST_TO_STATION<=8`; `BHV_SETTINGS` reports the new point but is not graded. | Ignoring the update leaves the vehicle near the old point and can satisfy the same relative-distance threshold. | Compare the point fields in `BHV_SETTINGS` with `(24,-121)` and/or grade absolute NAV position near the new point. | Open |
| stationkeep_h01 | `bad_point_update_recover_pass` | The malformed point update is rejected, emits the expected warning, and the later valid point restores normal operation. | Any warning plus final `DIST_TO_STATION<=8` and no error. | The warning payload is not tied to the bad point, and the initial point is already valid, so ignoring the valid recovery update can pass. | Compare the warning text, start from a distinct initial point or no point, and grade the final point from `BHV_SETTINGS` or NAV position. | Open |
| stationkeep_h01 | `visual_hints_pass` | The configured yellow/cyan colors and size `2` are accepted and published in the station visualization. | The unchanged baseline distance and no-warning/no-error evaluator. | Visual hints are never observed; ignoring every hint can pass. | Capture and compare the relevant `VIEW_POINT`/visual publication fields or another behavior-owned visualization output. | Open |
| stationkeep_h01 | `bad_update_fail` | A malformed runtime point update is what prevents a stationless behavior from becoming active. | The behavior starts without a point and ends with `DIST_TO_STATION>=100`, `PSKEEP_MODE=UNKNOWN`, and no error. | The case is already inactive before the bad update; omitting the update produces the same evidence. | Start from a runtime-initializable state or pair a valid update control with the malformed update, then compare the warning and active/inactive transition. | Open |
| stationkeep_h01 | `bad_point_fail`, `bad_outer_radius_fail`, `bad_inner_radius_fail`, `bad_hibernation_radius_fail`, `bad_outer_speed_fail`, `bad_transit_speed_fail`, `bad_extra_speed_fail`, `bad_center_activate_fail`, `bad_swing_time_fail` | Each malformed value is rejected for its specific parameter. | All cases require only inactive outputs (`DIST_TO_STATION>=100`, `PSKEEP_MODE=UNKNOWN`) and no `BHV_ERROR`; warning and settings are not graded. | The evidence shows StationKeep did not run but does not identify which parameter was rejected or require a diagnostic. | Compare a parameter-specific warning/error and, where available, require the exact rejected field to be absent from accepted `BHV_SETTINGS`. | Open |
| trail_h01 | `absolute_west_pass`, `relative_port_pass`, `relative_starboard_pass`, `lead_port_turn_pass`, `lead_turn_angle_update_pass`, `runtime_angle_update_pass` | Each absolute, relative, or runtime angle selects the intended world-relative trail-point location. | Generic `TRAIL_DISTANCE` upper bounds plus active pursuit/relevance; the turn evaluators additionally grade only Ben's post-turn position. | `TRAIL_DISTANCE` is distance to whatever trail point the implementation chose, so ignoring or misapplying the configured angle can still pass. | Grade Abe's absolute position relative to Ben or publish the computed trail point/angle and compare it with the expected geometry. | Open |
| trail_h01 | `short_trail_range_pass`, `long_trail_range_pass`, `mod_trail_range_plus_pass`, `mod_trail_range_pct_pass`, `mod_trail_range_floor_pass`, `runtime_range_extend_pass`, `runtime_mod_range_plus_pass`, `runtime_mod_range_pct_pass`, `runtime_mod_range_floor_pass` | Each static or runtime range setting produces the intended desired separation, including the 10-meter floor. | Final `TRAIL_DISTANCE` bounds measure error to the implementation's computed trail point; the configured/effective range is not published or graded. | An ignored or incorrectly computed desired range can still yield a small `TRAIL_DISTANCE` to a different trail point. | Publish and compare the effective trail range or grade Abe-Ben separation against the expected value, with explicit 10-meter floor controls. | Open |
| trail_h01 | `tight_radius_pass`, `wide_radius_pass`, `inside_nm_radius_pass`, `outside_nm_radius_pass` | The configured `radius` and `nm_radius` select the intended pursuit region and transition behavior. | Generic distance, pursuit, and relevance conditions; `REGION` is report-only. | Ignoring the radius settings can pass, and the inside/outside region named by each case is not required. | Require the expected `REGION` value and add timed transition evidence around each radius boundary. | Open |
| trail_h01 | `pwt_outer_active_pass` | `pwt_outer_dist=250` is what keeps pursuit relevant while the observed contact range remains below 250. | `TRAIL_RANGE<250`, positive relevance, active pursuit, and bounded trail distance. | The default `pwt_outer_dist=0` is unrestricted, so ignoring the case patch satisfies the same evidence. | Pair with a smaller threshold on identical geometry or grade accepted settings, and prove relevance changes only across the configured boundary. | Open |
| trail_h01 | `runtime_bad_update_recover_pass` | Invalid `trail_range=-5` is rejected with its diagnostic and the later valid `45` restores the intended range. | Any warning plus final generic pursuit/relevance and distance conditions. | The warning is not tied to the invalid update, and the behavior starts at `trail_range=45`, so ignoring the recovery update can pass. | Compare the warning payload, start from a distinct valid range, and grade the final effective range after recovery. | Open |
| trail_h01 | `no_extrapolate_pass` | `extrapolate=false` changes the contact-position source used for the trail point. | The generic late distance, pursuit, relevance, and no-error grade. | Ignoring the extrapolation setting can pass under the smooth target motion. | Use sparse/aged contact reports and grade a position or trail-point result that distinguishes projected from unprojected contact state. | Open |
| trail_h01 | `no_alert_request_pass` | `no_alert_request=true` suppresses `BCM_ALERT_REQUEST`. | The default pursuit/relevance/distance grade; no absence condition observes the request variable. | A request may still be published without failing the case. | Latch any `BCM_ALERT_REQUEST` and require it to remain absent while retaining active pursuit from existing contact data. | Open |
| trail_h01 | `auto_alert_request_pass` | The spawn-templated behavior publishes the correct contact-manager subscription request. | Any post to `BCM_ALERT_REQUEST` sets the pass latch. | The payload is not compared, so an incorrect request passes. | Compare contact name, alert range, alert variable, and any other stable request fields before grading pursuit. | Open |
| trail_h01 | `missing_contact_warn_pass`, `missing_contact_fail`, `missing_contact_param_fail` | Each unavailable/omitted contact produces the policy-specific Trail diagnostic. | Any warning or error publication satisfies the corresponding latch. | Unrelated diagnostics pass and the two error cases are not distinguished. | Compare exact missing-contact and missing-parameter payloads, retaining the no-error assertion in the warning-only case. | Open |
| trail_h01 | `bad_trail_angle_type_fail`, `bad_trail_angle_fail`, `bad_trail_range_fail`, `bad_mod_trail_range_pct_fail`, `bad_radius_fail`, `bad_nm_radius_fail`, `bad_pwt_outer_dist_fail`, `bad_decay_fail`, `bad_time_on_leg_fail` | Each malformed value is rejected for its specific parameter. | All cases require only unchanged inactive sentinel outputs and no `BHV_ERROR`; warnings are report-only. | The evidence proves Trail did not run but neither requires a diagnostic nor identifies the rejected field. | Require a parameter-specific warning/error and compare any accepted-settings publication or helm state that distinguishes each rejection. | Open |
| waypoint_h01 | `single_point_arrival_pass`, `endflag_pass` | The endflag case uniquely tests `WAYPOINT_END=true` beyond ordinary single-point completion. | Both cases use the identical stem and identical base evaluator, which already requires `WAYPOINT_END=true`. | The two cases are exact duplicates apart from the case name. | Remove one case or give the endflag case a distinct endflag payload and evaluator. | Open |
| waypoint_h01 | `multi_point_sequence_pass`, `wptflag_pass` | The wptflag case uniquely validates waypoint-hit publication on the three-point route. | Both use the identical route; the base evaluator for `multi_point_sequence_pass` already requires `WPT_HIT=true`, while the special wptflag evaluator is weaker. | The wptflag case adds no coverage and drops cycle/end assertions. | Remove the duplicate or make it grade a distinct payload/count for each waypoint hit. | Open |
| waypoint_h01 | `xpoints_update_pass` | The same-length `xpoints` update replaces the second route point with `(42,-103)` while preserving traversal state. | Final generic completion flags; final index and distance are report-only, and absolute NAV position is absent. | Ignoring the update and completing the original horizontal route passes. | Grade the updated route/end position or publish and compare the active point list after the update. | Open |
| waypoint_h01 | `capture_radius_large_pass`, `capture_line_absolute_pass`, `slip_radius_pass` | Completion occurs specifically through large-radius, absolute-line, or slip-radius capture. | Untimed final completion flags only. | Ordinary navigation to the point can eventually satisfy all three, so the evaluator does not identify the capture mechanism. | Add tight timing/position evidence and capture-mode telemetry; use geometries where only the intended mechanism can complete. | Open |
| waypoint_h01 | `order_reverse_pass`, `currix_start_pass`, `greedy_tour_pass` | The route is traversed in reverse, starts at index one, or is reordered greedily. | Only final completion flags; `WPT_INDEX` is report-only and no visit sequence is captured. | Any order that eventually visits the points passes. | Record waypoint-hit index/coordinate history and compare the exact expected sequence. | Open |
| waypoint_h01 | `lead_to_start_pass`, `lead_condition_pass`, `end_spd_slowdown_pass`, `speed_alt_update_pass`, `reset_on_idle_pass`, `ipf_type_roc_pass`, `ipf_type_zaic_spd_pass` | Each parameter or runtime event changes the intended path, speed, objective type, or reset behavior. | Generic final completion flags only. | Ignoring the distinct setting/event can still complete the route. | Add parameter-specific timing, speed, path, objective/debug, or pre/post-idle state evidence and mutation controls. | Open |
| waypoint_h01 | `repeat_once_cycle_pass`, `cycle_index_output_pass` | `repeat=1` completes two cycles rather than the default one. | The first case requires only a cycle flag; the second requires `CYCLE_INDEX>=1`, which the default `repeat=0` run also reaches. | Ignoring `repeat=1` can satisfy both grades. | Require `CYCLE_INDEX=2` and, ideally, capture two complete waypoint sequences before the done flag. | Open |
| waypoint_h01 | `custom_status_vars_pass` | `post_suffix=CUSTOM` applies to status, index, cycle index, and distance outputs with correct values. | Custom index and two custom distance variables are only checked for broad numeric signs; custom status and cycle index are not graded. | Missing status/cycle outputs or numerically incorrect distance values can pass. | Require all suffixed variables, compare status/index/cycle values, and use numeric distance bands from the known geometry. | Open |
| waypoint_h01 | `efficiency_measure_pass` | Distance and time efficiency values are numerically correct for the known three-leg route. | Both values need only be greater than zero. | Any positive constants pass. | Compute expected bands from route and elapsed travel, or cross-check the published values against logged path length and time. | Open |
| waypoint_h01 | `slow_speed_no_arrival_pass` | Speed `0.35` produces bounded forward progress without arrival by 10 seconds. | No completion flags and `WPT_DIST_TO_NEXT>=40`. | A stationary vehicle or inactive behavior can satisfy the same distance floor. | Add an upper distance bound below the initial 60 meters and require observed NAV speed/motion near `0.35`. | Open |
| waypoint_h01 | `malformed_update_fail` | The malformed `points` update is actively rejected with a diagnostic. | Same no-completion flags as the no-update case; warning/error payloads are not checked. | Silently ignoring every update produces the same result. | Require the malformed-update warning and pair with the valid dynamic-points case at the same timing. | Open |
| waypoint_h01 | `bad_xpoints_size_fail` | A one-point `xpoints` update is rejected because it does not match the active two-point route. | No completion by eight seconds. | Even accepting the new point at `(30,-121)` would not necessarily complete by eight seconds from the `(-18,-121)` start. | Use an invalid replacement point inside immediate capture range and require the original route/list to remain active plus a size-mismatch warning. | Open |
| waypoint_h01 | `bad_speed_fail`, `bad_capture_line_fail`, `bad_capture_radius_fail`, `bad_slip_radius_fail`, `bad_order_fail`, `bad_repeat_fail`, `bad_lead_damper_fail`, `bad_lead_condition_fail`, `bad_end_spd_fail`, `bad_patience_fail`, `bad_efficiency_measure_fail` | Each malformed value causes a parameter-specific helm `MALCONFIG`. | `mailflag=@IVPHELM_STATE#HELM_MALCONFIG=true` sets the latch on any helm-state publication; its payload is not compared. | A normal helm-state publication can pass every negative case, and no evidence identifies the rejected parameter. | Require `IVPHELM_STATE=MALCONFIG` and compare a parameter-specific `BHV_ERROR` or warning payload. | Open |
| ufld_contact_range_sensor_h01 | `ground_truth_gaussian_unsupported_no_gt_pass`, `ground_truth_no_algorithm_absent_pass`, `ground_truth_false_no_gt_pass` | No ground-truth output is published in either short or requester-specific long form. | Each case latches only `CRS_RANGE_REPORT_GT`; none watches `CRS_RANGE_REPORT_GT_ALPHA`. | A stray long-form ground-truth report can pass. | Add an absence latch for the expected requester-specific GT variable and require both latches to remain false. | Open |
| ufld_contact_range_sensor_h01 | `sensor_arc_wrap_accept_pass`, `sensor_arc_full_accept_pass` | The wraparound and `360` arc syntaxes are interpreted correctly, not merely accepted. | Both use the baseline forward contact and exact report/pulse grade. | Ignoring either arc setting leaves the default unrestricted sensor and produces the same result. | Add outside-arc blocking controls for the wrap case and multiple bearings for the full-circle case; confirm with arc-ignored mutations. | Open |
| ufld_beacon_range_sensor_h01 | `brs_ground_truth_false_no_gt_pass` | `ground_truth=false` suppresses both short and requester-specific long ground-truth output while ordinary reports remain enabled. | The case requires an ordinary short report and latches only `BRS_RANGE_REPORT_GT`. | A stray `BRS_RANGE_REPORT_GT_ALPHA` can be published without failing the case. | Add an absence latch for `BRS_RANGE_REPORT_GT_ALPHA` and require both ground-truth channels to remain absent. | Open |
| ufld_beacon_range_sensor_h01 | `beacon_reply_push_blocks_pass`, `request_path_pull_blocks_pass` | Beacon push distance governs the reply leg while node push/beacon pull distance governs the request leg. | Each case makes one direction 10 meters and the other 100 meters at a 50-meter separation, then requires the same request pulse and absence of a report. | Swapping the request- and reply-leg interpretations produces the same result in both cases, so the pair proves a directional limit exists but not which setting controls which leg. | Add asymmetric intermediate telemetry or a controlled relay/acknowledgement that proves whether the beacon received the request before grading the reply; mutation-test swapped push/pull roles. | Open |
| ufld_collision_detect_h01 | `range_normalization_params_pass` | Invalid ordering `collision=6`, `near_miss=3`, `encounter=4` is normalized to `6/6/6` and that exact normalized summary is published. | A 5-meter collision report proves the event was admitted and classified under the 6-meter collision range, but the params assertion accepts any `COLLISION_DETECT_PARAMS` payload. | The evaluator does not prove the published values are `6/6/6`, and the collision rank cannot independently identify the normalized near-miss value. | Compare the exact parameter summary `collision_range=6, near_miss_range=6, encounter_range=6` and retain the 5-meter event grade. | Open |
| ufld_collob_detect_h01 | `named_clear_only_removes_target_pass` | `KNOWN_OBSTACLE_CLEAR=obs1` removes `obs1` and leaves `obs2` intact. | After the clear, Alpha encounters only `obs2`, and exact flags prove `obs2` remains active. | Ignoring the clear entirely also leaves `obs2` active and passes; the case never probes whether `obs1` was removed. | After the named clear, drive a second vehicle through `obs1` and require no distance/flag output while retaining the exact `obs2` encounter. | Open |
| ufld_collob_detect_h01 | `range_normalization_collision_pass` | Invalid `collision=6`, `near_miss=3`, `encounter=4` ordering normalizes both outer thresholds to `6`. | A 5.5-meter collision and encounter flag proves the event was admitted under an outer range above 5.5 and ranked under the 6-meter collision threshold. | An implementation that expands only `encounter_dist` while leaving `near_miss_dist=3` produces the same flags, so near-miss normalization is not demonstrated. | Add an observable parameter summary or a paired event that distinguishes a normalized 6-meter near-miss threshold from the original 3-meter value without falling inside the collision threshold. | Open |
| ufld_scope_h01 | all eight cases | The rendered AppCast has the intended headers, keyed rows, and field values as a table. | `zlaunch.sh` searches the final serialized AppCast line for required or forbidden text tokens; it does not parse headers, rows, or cell boundaries. | Tokens can occur in the wrong row or region and still pass; several checks omit a value needed to prove the named contract, such as Alpha's `survey` value in `multi_vehicle_rows_pass` and the `MODE` header in `later_missing_field_replaces_value_pass`. | Decode the AppCast report, extract the rendered table, and compare an exact header list plus keyed row/cell map per case. | Open |
| ufld_scope_h01 | `invalid_scope_ignored_pass` | The incomplete scope definition is rejected or ignored while the following valid definition remains active. | The final AppCast need only contain the token `MODE`. | The valid line can produce `MODE` even if the malformed line is also accepted or mishandled; no diagnostic or absence evidence identifies its treatment. | Require the specific configuration warning and compare the exact single-column table produced by the valid scope. | Open |
| ufield_comms_h01 | `runtime_stealth_block_pass` | The runtime stealth update blocks both Abe's later message and Abe's later node reports while leaving Ben-to-Abe traffic available. | Exact message count/value proves the later Abe message is blocked; report latches only prove that each direction delivered at least once at some point in the mission. | Abe's post-update reports may continue reaching Ben without failing because the earlier report already set `REPORT_ABE_TO_BEN=true`. | Reset or count directional report latches around the update and require no Abe-to-Ben increment after `UNC_STEALTH`, while requiring a Ben-to-Abe increment. | Open |
| ufield_comms_h01 | `shared_node_reports_numeric_pass` | Numeric `shared_node_reports=0.1` controls a 0.1-second shared-report interval rather than merely enabling sharing. | The evaluator requires only that at least one `NODE_REPORT_UNC` appear. | Treating every nonzero numeric value as boolean `true`, or publishing at the wrong interval, passes. | Count timestamped shared reports over a bounded window and compare the cadence with the configured interval, with a boolean-true control. | Open |
| ufield_comms_h02 | `broker_handshake_pass` | Both node brokers publish the expected `NODE_PSHARE_VARS` alias set as part of their successful handshakes. | Both nodes have independent ping/ack/vack log evidence, but the mission evaluator compares only the latest shoreside `NODE_PSHARE_VARS` value. | A correct alias publication from one node can mask a missing or incorrect publication from the other. | Check `NODE_PSHARE_VARS` independently in both Abe and Ben logs, as the node-minimal case already does. | Open |
| ufield_comms_h03 | `startup_invalid_bad_param_valid_route_pass`, `invalid_runtime_bad_param_recover_pass` | A `shore_route=...` payload is rejected and does not consume a route key before the valid `pshare_route=...`. | The supplemental check only requires no `PSHARE_CMD` text containing `shore_route` and a later `status=ok,key=0`. | An implementation can incorrectly accept `shore_route`, normalize its output to `route=...`, and establish the live endpoint as key `0`; the forbidden input token would disappear and both cases would pass. | Assert the number and timing of broker-ping pShare commands, require the malformed input to produce no key, and make its endpoint distinct from the later valid route. | Open |
| ufield_comms_h03 | `late_runtime_tryhost_blocks_delivery_fail` | Traffic sent before the late route—including direct message, acknowledgement, and node reports—remains missed even though the brokers later recover. | The evaluator requires node count two and only `DIRECT_MSG_DELIVERED=false`; the supplemental check proves both late `status=ok,key=0` acknowledgements. | Acknowledgement or report delivery can occur and the case still passes, contrary to the broader route-delivery claim. | Require the acknowledgement and both directional report latches to remain false, or narrow the intended contract explicitly to the one pre-route direct message. | Open |
| ufield_comms_h03 | `shore_vnode_duplicate_ignored_pass` | Repeating Abe's vnode route is deduplicated rather than emitted twice. | The case requires only some valid discovery command, both successful broker acknowledgements, and the ordinary mission grade. | Emitting duplicate pShare commands for Abe passes; no command count, route-key evidence, or duplicate diagnostic is checked. | Count exact vnode `PSHARE_CMD` routes and require one command per unique endpoint, optionally checking the duplicate warning. | Open |
| ufld_obstacle_sim_h01 | `refresh_interval_repost_pass`, `refresh_interval_post_points_no_given_pass` | `refresh_interval=0.6` controls the expected refresh cadence. | Each case evaluates five seconds after the first trigger and requires only at least three known-obstacle posts; the non-point case also requires at least three given-obstacle posts. | Any substantially different interval that happens to produce three posts in the broad window passes; the observed count is not compared with elapsed time. | Count timestamped publications in a controlled window and compare their spacing or exact count with the 0.6-second interval, allowing a small scheduler tolerance. | Open |
| ufld_obstacle_sim_h01 | `reset_request_inside_blocked_pass`, `reset_range_boundary_blocked_pass`, `reset_interval_wait_blocked_pass` | A blocked manual or automatic reset performs no clear and no obstacle republish/regeneration. | The cases require absence of `KNOWN_OBSTACLE_CLEAR` and retain only current truth values; none counts known/given publications after the blocking event. | A buggy reset that republishes the same `reuse_ids=true` obstacle without emitting a clear can pass. | Count truth publications before and after the reset/exit event and require no increment, in addition to the existing clear absence. | Open |

## Harness Inventory

| Target | Family | Cases | README | Page | Links |
| --- | --- | ---: | :---: | :---: | --- |
| cmgr_h01 | cmgr | 32 | [x] | [x] | [README](../harnesses/cmgr_harnesses/H01-cmgr_unit/README.md) · [page](harnesses/cmgr-unit.html) |
| cmgr_h02 | cmgr | 14 | [x] | [x] | [README](../harnesses/cmgr_harnesses/H02-cmgr_motion/README.md) · [page](harnesses/cmgr-motion.html) |
| obmgr_h01 | obmgr | 30 | [x] | [x] | [README](../harnesses/obmgr_harnesses/H01-obmgr_unit/README.md) · [page](harnesses/obmgr-unit.html) |
| obmgr_h02 | obmgr | 10 | [x] | [x] | [README](../harnesses/obmgr_harnesses/H02-obmgr_motion/README.md) · [page](harnesses/obmgr-motion.html) |
| pid_h01 | pid | 34 | [x] | [x] | [README](../harnesses/pid_harnesses/H01-pid_unit/README.md) · [page](harnesses/pid-unit.html) |
| pid_h02 | pid | 13 | [x] | [x] | [README](../harnesses/pid_harnesses/H02-pid_motion/README.md) · [page](harnesses/pid-motion.html) |
| usim_marine_h01 | usim_marine | 36 | [x] | [x] | [README](../harnesses/usim_marine_harnesses/H01-usim_marine_motion/README.md) · [page](harnesses/usim-marine-motion.html) |
| pnodereporter_h01 | pnodereporter | 33 | [x] | [x] | [README](../harnesses/pnodereporter_harnesses/H01-pnodereporter_unit/README.md) · [page](harnesses/pnodereporter-unit.html) |
| upokedb_h01 | upokedb | 28 | [x] | [x] | [README](../harnesses/upokedb_harnesses/H01-upokedb_unit/README.md) · [page](harnesses/upokedb-unit.html) |
| uxms_h01 | uxms | 32 | [x] | [x] | [README](../harnesses/uxms_harnesses/H01-uxms_unit/README.md) · [page](harnesses/uxms-unit.html) |
| uquerydb_h01 | uquerydb | 30 | [x] | [x] | [README](../harnesses/uquerydb_harnesses/H01-uquerydb_unit/README.md) · [page](harnesses/uquerydb-unit.html) |
| pechovar_h01 | pechovar | 33 | [x] | [x] | [README](../harnesses/pechovar_harnesses/H01-pechovar_unit/README.md) · [page](harnesses/pechovar-unit.html) |
| utimerscript_h01 | utimerscript | 33 | [x] | [x] | [README](../harnesses/utimerscript_harnesses/H01-utimerscript_unit/README.md) · [page](harnesses/utimerscript-unit.html) |
| pdeadmanpost_h01 | pdeadmanpost | 20 | [x] | [x] | [README](../harnesses/pdeadmanpost_harnesses/H01-pdeadmanpost_unit/README.md) · [page](harnesses/pdeadmanpost-unit.html) |
| pspoofnode_h01 | pspoofnode | 33 | [x] | [x] | [README](../harnesses/pspoofnode_harnesses/H01-pspoofnode_unit/README.md) · [page](harnesses/pspoofnode-unit.html) |
| utermcommand_h01 | utermcommand | 15 | [x] | [x] | [README](../harnesses/utermcommand_harnesses/H01-utermcommand_unit/README.md) · [page](harnesses/utermcommand-unit.html) |
| psearchgrid_h01 | psearchgrid | 23 | [x] | [x] | [README](../harnesses/psearchgrid_harnesses/H01-psearchgrid_unit/README.md) · [page](harnesses/psearchgrid-unit.html) |
| depth_constant_h01 | depth_behavior | 19 | [x] | [x] | [README](../harnesses/depth_behavior_harnesses/H01-constant_depth_motion/README.md) · [page](harnesses/constant-depth-motion.html) |
| depth_goto_h02 | depth_behavior | 18 | [x] | [x] | [README](../harnesses/depth_behavior_harnesses/H02-goto_depth_motion/README.md) · [page](harnesses/goto-depth-motion.html) |
| depth_periodic_surface_h03 | depth_behavior | 24 | [x] | [x] | [README](../harnesses/depth_behavior_harnesses/H03-periodic_surface_motion/README.md) · [page](harnesses/periodic-surface-motion.html) |
| depth_max_h04 | depth_behavior | 15 | [x] | [x] | [README](../harnesses/depth_behavior_harnesses/H04-max_depth_motion/README.md) · [page](harnesses/max-depth-motion.html) |
| depth_min_altitude_h05 | depth_behavior | 13 | [x] | [x] | [README](../harnesses/depth_behavior_harnesses/H05-min_altitude_motion/README.md) · [page](harnesses/min-altitude-motion.html) |
| colregs_h01 | colregs | 22 | [x] | [x] | [README](../harnesses/colregs_harnesses/H01-colregs_classification/README.md) · [page](harnesses/colregs-classification.html) |
| colregs_h02 | colregs | 56 | [x] | [x] | [README](../harnesses/colregs_harnesses/H02-colregs_thresholds/README.md) · [page](harnesses/colregs-thresholds.html) |
| colregs_h03 | colregs | 23 | [x] | [x] | [README](../harnesses/colregs_harnesses/H03-colregs_execution/README.md) · [page](harnesses/colregs-execution.html) |
| colregs_h04 | colregs | 32 | [x] | [x] | [README](../harnesses/colregs_harnesses/H04-colregs_parameters/README.md) · [page](harnesses/colregs-parameters.html) |
| collision_h01 | collision_behavior | 21 | [x] | [x] | [README](../harnesses/collision_behavior_harnesses/H01-collision_behavior_motion/README.md) · [page](harnesses/collision-behavior-motion.html) |
| convoy_h01 | convoy_behavior | 48 | [x] | [x] | [README](../harnesses/convoy_behavior_harnesses/H01-convoy_behavior_motion/README.md) · [page](harnesses/convoy-behavior-motion.html) |
| cutrange_h01 | cutrange_behavior | 39 | [x] | [x] | [README](../harnesses/cutrange_behavior_harnesses/H01-cutrange_behavior_motion/README.md) · [page](harnesses/cutrange-behavior-motion.html) |
| fixedturn_h01 | fixedturn_behavior | 24 | [x] | [x] | [README](../harnesses/fixedturn_behavior_harnesses/H01-fixedturn_behavior_motion/README.md) · [page](harnesses/fixedturn-behavior-motion.html) |
| periodic_speed_h01 | periodic_speed_behavior | 24 | [x] | [x] | [README](../harnesses/periodic_speed_behavior_harnesses/H01-periodic_speed_behavior_motion/README.md) · [page](harnesses/periodic-speed-behavior-motion.html) |
| zigzag_h01 | zigzag_behavior | 35 | [x] | [x] | [README](../harnesses/zigzag_behavior_harnesses/H01-zigzag_behavior_motion/README.md) · [page](harnesses/zigzag-behavior-motion.html) |
| legrun_h01 | legrun_behavior | 33 | [x] | [x] | [README](../harnesses/legrun_behavior_harnesses/H01-legrun_behavior_motion/README.md) · [page](harnesses/legrun-behavior-motion.html) |
| memoryturnlimit_h01 | memoryturnlimit_behavior | 21 | [x] | [x] | [README](../harnesses/memoryturnlimit_behavior_harnesses/H01-memoryturnlimit_behavior_motion/README.md) · [page](harnesses/memoryturnlimit-behavior-motion.html) |
| timer_h01 | timer_behavior | 18 | [x] | [x] | [README](../harnesses/timer_behavior_harnesses/H01-timer_behavior_motion/README.md) · [page](harnesses/timer-behavior-motion.html) |
| testfailure_h01 | testfailure_behavior | 9 | [x] | [x] | [README](../harnesses/testfailure_behavior_harnesses/H01-testfailure_behavior_unit/README.md) · [page](harnesses/testfailure-behavior-unit.html) |
| hostinfo_h01 | hostinfo | 7 | [x] | [x] | [README](../harnesses/hostinfo_harnesses/H01-hostinfo_unit/README.md) · [page](harnesses/hostinfo-unit.html) |
| loadwatch_h01 | loadwatch | 12 | [x] | [x] | [README](../harnesses/loadwatch_harnesses/H01-loadwatch_unit/README.md) · [page](harnesses/loadwatch-unit.html) |
| processwatch_h01 | processwatch | 7 | [x] | [x] | [README](../harnesses/processwatch_harnesses/H01-processwatch_unit/README.md) · [page](harnesses/processwatch-unit.html) |
| pshare_h01 | pshare | 13 | [x] | [x] | [README](../harnesses/pshare_harnesses/H01-pshare_unit/README.md) · [page](harnesses/pshare-unit.html) |
| pshare_h02 | pshare | 12 | [x] | [x] | [README](../harnesses/pshare_harnesses/H02-pshare_topology/README.md) · [page](harnesses/pshare-topology.html) |
| plogger_h01 | plogger | 10 | [x] | [x] | [README](../harnesses/plogger_harnesses/H01-plogger_unit/README.md) · [page](harnesses/plogger-unit.html) |
| pantler_h01 | pantler | 6 | [x] | [x] | [README](../harnesses/pantler_harnesses/H01-pantler_unit/README.md) · [page](harnesses/pantler-unit.html) |
| pmissioneval_h01 | pmissioneval | 17 | [x] | [x] | [README](../harnesses/mission_utility_harnesses/H01-pmissioneval_unit/README.md) · [page](harnesses/pmissioneval-unit.html) |
| pmissionhash_h01 | pmissionhash | 4 | [x] | [x] | [README](../harnesses/mission_utility_harnesses/H02-pmissionhash_unit/README.md) · [page](harnesses/pmissionhash-unit.html) |
| umayfinish_h01 | umayfinish | 4 | [x] | [x] | [README](../harnesses/mission_utility_harnesses/H03-umayfinish_unit/README.md) · [page](harnesses/umayfinish-unit.html) |
| loiter_h01 | loiter_behavior | 34 | [x] | [x] | [README](../harnesses/loiter_behavior_harnesses/H01-loiter_behavior_motion/README.md) · [page](harnesses/loiter-behavior-motion.html) |
| obstacle_behavior_h01 | obstacle_behavior | 21 | [x] | [x] | [README](../harnesses/obstacle_behavior_harnesses/H01-obstacle_behavior_motion/README.md) · [page](harnesses/obstacle-behavior-motion.html) |
| opregion_h01 | opregion | 25 | [x] | [x] | [README](../harnesses/opregion_harnesses/H01-opregion_safety/README.md) · [page](harnesses/opregion-safety.html) |
| p01_obstacle | performance | 3 | [x] | [x] | [README](../harnesses/performance_harnesses/P01-obstacle_gauntlet/README.md) · [page](harnesses/performance-obstacle-gauntlet.html) |
| shadow_h01 | shadow_behavior | 35 | [x] | [x] | [README](../harnesses/shadow_behavior_harnesses/H01-shadow_behavior_motion/README.md) · [page](harnesses/shadow-behavior-motion.html) |
| stationkeep_h01 | stationkeep_behavior | 34 | [x] | [x] | [README](../harnesses/stationkeep_behavior_harnesses/H01-stationkeep_behavior_motion/README.md) · [page](harnesses/stationkeep-behavior-motion.html) |
| trail_h01 | trail_behavior | 42 | [x] | [x] | [README](../harnesses/trail_behavior_harnesses/H01-trail_behavior_motion/README.md) · [page](harnesses/trail-behavior-motion.html) |
| waypoint_h01 | waypoint_behavior | 43 | [x] | [x] | [README](../harnesses/waypoint_behavior_harnesses/H01-waypoint_behavior_motion/README.md) · [page](harnesses/waypoint-behavior-motion.html) |
| p02_colregs | colregs, performance | 3 | [x] | [x] | [README](../harnesses/performance_harnesses/P02-colregs_joust/README.md) · [page](harnesses/performance-colregs-joust.html) |
| p03_colregs | colregs, performance | 4 | [x] | [x] | [README](../harnesses/performance_harnesses/P03-colregs_traffic_ring/README.md) · [page](harnesses/performance-traffic-ring.html) |
| ufld_pathcheck_h01 | ufld_pathcheck | 17 | [x] | [x] | [README](../harnesses/ufld_pathcheck_harnesses/H01-ufld_pathcheck_unit/README.md) · [page](harnesses/ufld-pathcheck-unit.html) |
| ufld_message_handler_h01 | ufld_message_handler | 26 | [x] | [x] | [README](../harnesses/ufld_message_handler_harnesses/H01-ufld_message_handler_unit/README.md) · [page](harnesses/ufld-message-handler-unit.html) |
| ufld_contact_range_sensor_h01 | ufld_contact_range_sensor | 34 | [x] | [x] | [README](../harnesses/ufld_contact_range_sensor_harnesses/H01-ufld_contact_range_sensor_unit/README.md) · [page](harnesses/ufld-contact-range-sensor-unit.html) |
| ufld_beacon_range_sensor_h01 | ufld_beacon_range_sensor | 20 | [x] | [x] | [README](../harnesses/ufld_beacon_range_sensor_harnesses/H01-ufld_beacon_range_sensor_unit/README.md) · [page](harnesses/ufld-beacon-range-sensor-unit.html) |
| ufld_collision_detect_h01 | ufld_collision_detect | 20 | [x] | [x] | [README](../harnesses/ufld_collision_detect_harnesses/H01-ufld_collision_detect_unit/README.md) · [page](harnesses/ufld-collision-detect-unit.html) |
| ufld_collob_detect_h01 | ufld_collob_detect | 14 | [x] | [x] | [README](../harnesses/ufld_collob_detect_harnesses/H01-ufld_collob_detect_unit/README.md) · [page](harnesses/ufld-collob-detect-unit.html) |
| ufld_scope_h01 | ufld_scope | 8 | [x] | [x] | [README](../harnesses/ufld_scope_harnesses/H01-ufld_scope_unit/README.md) · [page](harnesses/ufld-scope-unit.html) |
| ufield_comms_h01 | ufield_comms | 35 | [x] | [x] | [README](../harnesses/ufield_comms_harnesses/H01-ufield_comms_unit/README.md) · [page](harnesses/ufield-comms-unit.html) |
| ufield_comms_h02 | ufield_comms | 12 | [x] | [x] | [README](../harnesses/ufield_comms_harnesses/H02-ufield_broker_bridge/README.md) · [page](harnesses/ufield-broker-bridge.html) |
| ufield_comms_h03 | ufield_comms | 25 | [x] | [x] | [README](../harnesses/ufield_comms_harnesses/H03-ufield_route_resilience/README.md) · [page](harnesses/ufield-route-resilience.html) |
| ufld_obstacle_sim_h01 | ufld_obstacle_sim | 49 | [x] | [x] | [README](../harnesses/ufld_obstacle_sim_harnesses/H01-ufld_obstacle_sim_unit/README.md) · [page](harnesses/ufld-obstacle-sim-unit.html) |
